// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"silan-backend/internal/ent/partentry"
	"silan-backend/internal/ent/partentrytranslation"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// PartEntryTranslation is the model entity for the PartEntryTranslation schema.
type PartEntryTranslation struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// PartEntryID holds the value of the "part_entry_id" field.
	PartEntryID string `json:"part_entry_id,omitempty"`
	// LanguageCode holds the value of the "language_code" field.
	LanguageCode string `json:"language_code,omitempty"`
	// LocalizedPayload holds the value of the "localized_payload" field.
	LocalizedPayload map[string]interface{} `json:"localized_payload,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PartEntryTranslationQuery when eager-loading is set.
	Edges        PartEntryTranslationEdges `json:"edges"`
	selectValues sql.SelectValues
}

// PartEntryTranslationEdges holds the relations/edges for other nodes in the graph.
type PartEntryTranslationEdges struct {
	// PartEntry holds the value of the part_entry edge.
	PartEntry *PartEntry `json:"part_entry,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// PartEntryOrErr returns the PartEntry value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PartEntryTranslationEdges) PartEntryOrErr() (*PartEntry, error) {
	if e.PartEntry != nil {
		return e.PartEntry, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: partentry.Label}
	}
	return nil, &NotLoadedError{edge: "part_entry"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PartEntryTranslation) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case partentrytranslation.FieldLocalizedPayload:
			values[i] = new([]byte)
		case partentrytranslation.FieldID, partentrytranslation.FieldPartEntryID, partentrytranslation.FieldLanguageCode:
			values[i] = new(sql.NullString)
		case partentrytranslation.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PartEntryTranslation fields.
func (pet *PartEntryTranslation) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case partentrytranslation.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				pet.ID = value.String
			}
		case partentrytranslation.FieldPartEntryID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field part_entry_id", values[i])
			} else if value.Valid {
				pet.PartEntryID = value.String
			}
		case partentrytranslation.FieldLanguageCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field language_code", values[i])
			} else if value.Valid {
				pet.LanguageCode = value.String
			}
		case partentrytranslation.FieldLocalizedPayload:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field localized_payload", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &pet.LocalizedPayload); err != nil {
					return fmt.Errorf("unmarshal field localized_payload: %w", err)
				}
			}
		case partentrytranslation.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				pet.CreatedAt = value.Time
			}
		default:
			pet.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PartEntryTranslation.
// This includes values selected through modifiers, order, etc.
func (pet *PartEntryTranslation) Value(name string) (ent.Value, error) {
	return pet.selectValues.Get(name)
}

// QueryPartEntry queries the "part_entry" edge of the PartEntryTranslation entity.
func (pet *PartEntryTranslation) QueryPartEntry() *PartEntryQuery {
	return NewPartEntryTranslationClient(pet.config).QueryPartEntry(pet)
}

// Update returns a builder for updating this PartEntryTranslation.
// Note that you need to call PartEntryTranslation.Unwrap() before calling this method if this PartEntryTranslation
// was returned from a transaction, and the transaction was committed or rolled back.
func (pet *PartEntryTranslation) Update() *PartEntryTranslationUpdateOne {
	return NewPartEntryTranslationClient(pet.config).UpdateOne(pet)
}

// Unwrap unwraps the PartEntryTranslation entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pet *PartEntryTranslation) Unwrap() *PartEntryTranslation {
	_tx, ok := pet.config.driver.(*txDriver)
	if !ok {
		panic("ent: PartEntryTranslation is not a transactional entity")
	}
	pet.config.driver = _tx.drv
	return pet
}

// String implements the fmt.Stringer.
func (pet *PartEntryTranslation) String() string {
	var builder strings.Builder
	builder.WriteString("PartEntryTranslation(")
	builder.WriteString(fmt.Sprintf("id=%v, ", pet.ID))
	builder.WriteString("part_entry_id=")
	builder.WriteString(pet.PartEntryID)
	builder.WriteString(", ")
	builder.WriteString("language_code=")
	builder.WriteString(pet.LanguageCode)
	builder.WriteString(", ")
	builder.WriteString("localized_payload=")
	builder.WriteString(fmt.Sprintf("%v", pet.LocalizedPayload))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(pet.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// PartEntryTranslations is a parsable slice of PartEntryTranslation.
type PartEntryTranslations []*PartEntryTranslation
