// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"silan-backend/internal/ent/statscachesource"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// StatsCacheSource is the model entity for the StatsCacheSource schema.
type StatsCacheSource struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// EntityType holds the value of the "entity_type" field.
	EntityType statscachesource.EntityType `json:"entity_type,omitempty"`
	// EntityID holds the value of the "entity_id" field.
	EntityID string `json:"entity_id,omitempty"`
	// Source holds the value of the "source" field.
	Source statscachesource.Source `json:"source,omitempty"`
	// Count holds the value of the "count" field.
	Count int `json:"count,omitempty"`
	// SyncedAt holds the value of the "synced_at" field.
	SyncedAt     time.Time `json:"synced_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*StatsCacheSource) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case statscachesource.FieldID, statscachesource.FieldCount:
			values[i] = new(sql.NullInt64)
		case statscachesource.FieldEntityType, statscachesource.FieldEntityID, statscachesource.FieldSource:
			values[i] = new(sql.NullString)
		case statscachesource.FieldSyncedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the StatsCacheSource fields.
func (scs *StatsCacheSource) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case statscachesource.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			scs.ID = int(value.Int64)
		case statscachesource.FieldEntityType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field entity_type", values[i])
			} else if value.Valid {
				scs.EntityType = statscachesource.EntityType(value.String)
			}
		case statscachesource.FieldEntityID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field entity_id", values[i])
			} else if value.Valid {
				scs.EntityID = value.String
			}
		case statscachesource.FieldSource:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field source", values[i])
			} else if value.Valid {
				scs.Source = statscachesource.Source(value.String)
			}
		case statscachesource.FieldCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field count", values[i])
			} else if value.Valid {
				scs.Count = int(value.Int64)
			}
		case statscachesource.FieldSyncedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field synced_at", values[i])
			} else if value.Valid {
				scs.SyncedAt = value.Time
			}
		default:
			scs.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the StatsCacheSource.
// This includes values selected through modifiers, order, etc.
func (scs *StatsCacheSource) Value(name string) (ent.Value, error) {
	return scs.selectValues.Get(name)
}

// Update returns a builder for updating this StatsCacheSource.
// Note that you need to call StatsCacheSource.Unwrap() before calling this method if this StatsCacheSource
// was returned from a transaction, and the transaction was committed or rolled back.
func (scs *StatsCacheSource) Update() *StatsCacheSourceUpdateOne {
	return NewStatsCacheSourceClient(scs.config).UpdateOne(scs)
}

// Unwrap unwraps the StatsCacheSource entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (scs *StatsCacheSource) Unwrap() *StatsCacheSource {
	_tx, ok := scs.config.driver.(*txDriver)
	if !ok {
		panic("ent: StatsCacheSource is not a transactional entity")
	}
	scs.config.driver = _tx.drv
	return scs
}

// String implements the fmt.Stringer.
func (scs *StatsCacheSource) String() string {
	var builder strings.Builder
	builder.WriteString("StatsCacheSource(")
	builder.WriteString(fmt.Sprintf("id=%v, ", scs.ID))
	builder.WriteString("entity_type=")
	builder.WriteString(fmt.Sprintf("%v", scs.EntityType))
	builder.WriteString(", ")
	builder.WriteString("entity_id=")
	builder.WriteString(scs.EntityID)
	builder.WriteString(", ")
	builder.WriteString("source=")
	builder.WriteString(fmt.Sprintf("%v", scs.Source))
	builder.WriteString(", ")
	builder.WriteString("count=")
	builder.WriteString(fmt.Sprintf("%v", scs.Count))
	builder.WriteString(", ")
	builder.WriteString("synced_at=")
	builder.WriteString(scs.SyncedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// StatsCacheSources is a parsable slice of StatsCacheSource.
type StatsCacheSources []*StatsCacheSource
