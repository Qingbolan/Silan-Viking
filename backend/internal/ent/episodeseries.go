// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"silan-backend/internal/ent/episodeseries"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// EpisodeSeries is the model entity for the EpisodeSeries schema.
type EpisodeSeries struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// Slug holds the value of the "slug" field.
	Slug string `json:"slug,omitempty"`
	// Title holds the value of the "title" field.
	Title string `json:"title,omitempty"`
	// Description holds the value of the "description" field.
	Description *string `json:"description,omitempty"`
	// Status holds the value of the "status" field.
	Status episodeseries.Status `json:"status,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the EpisodeSeriesQuery when eager-loading is set.
	Edges        EpisodeSeriesEdges `json:"edges"`
	selectValues sql.SelectValues
}

// EpisodeSeriesEdges holds the relations/edges for other nodes in the graph.
type EpisodeSeriesEdges struct {
	// Episodes holds the value of the episodes edge.
	Episodes []*Episode `json:"episodes,omitempty"`
	// Translations holds the value of the translations edge.
	Translations []*EpisodeSeriesTranslation `json:"translations,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// EpisodesOrErr returns the Episodes value or an error if the edge
// was not loaded in eager-loading.
func (e EpisodeSeriesEdges) EpisodesOrErr() ([]*Episode, error) {
	if e.loadedTypes[0] {
		return e.Episodes, nil
	}
	return nil, &NotLoadedError{edge: "episodes"}
}

// TranslationsOrErr returns the Translations value or an error if the edge
// was not loaded in eager-loading.
func (e EpisodeSeriesEdges) TranslationsOrErr() ([]*EpisodeSeriesTranslation, error) {
	if e.loadedTypes[1] {
		return e.Translations, nil
	}
	return nil, &NotLoadedError{edge: "translations"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*EpisodeSeries) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case episodeseries.FieldID, episodeseries.FieldSlug, episodeseries.FieldTitle, episodeseries.FieldDescription, episodeseries.FieldStatus:
			values[i] = new(sql.NullString)
		case episodeseries.FieldCreatedAt, episodeseries.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the EpisodeSeries fields.
func (es *EpisodeSeries) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case episodeseries.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				es.ID = value.String
			}
		case episodeseries.FieldSlug:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field slug", values[i])
			} else if value.Valid {
				es.Slug = value.String
			}
		case episodeseries.FieldTitle:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field title", values[i])
			} else if value.Valid {
				es.Title = value.String
			}
		case episodeseries.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				es.Description = new(string)
				*es.Description = value.String
			}
		case episodeseries.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				es.Status = episodeseries.Status(value.String)
			}
		case episodeseries.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				es.CreatedAt = value.Time
			}
		case episodeseries.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				es.UpdatedAt = value.Time
			}
		default:
			es.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the EpisodeSeries.
// This includes values selected through modifiers, order, etc.
func (es *EpisodeSeries) Value(name string) (ent.Value, error) {
	return es.selectValues.Get(name)
}

// QueryEpisodes queries the "episodes" edge of the EpisodeSeries entity.
func (es *EpisodeSeries) QueryEpisodes() *EpisodeQuery {
	return NewEpisodeSeriesClient(es.config).QueryEpisodes(es)
}

// QueryTranslations queries the "translations" edge of the EpisodeSeries entity.
func (es *EpisodeSeries) QueryTranslations() *EpisodeSeriesTranslationQuery {
	return NewEpisodeSeriesClient(es.config).QueryTranslations(es)
}

// Update returns a builder for updating this EpisodeSeries.
// Note that you need to call EpisodeSeries.Unwrap() before calling this method if this EpisodeSeries
// was returned from a transaction, and the transaction was committed or rolled back.
func (es *EpisodeSeries) Update() *EpisodeSeriesUpdateOne {
	return NewEpisodeSeriesClient(es.config).UpdateOne(es)
}

// Unwrap unwraps the EpisodeSeries entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (es *EpisodeSeries) Unwrap() *EpisodeSeries {
	_tx, ok := es.config.driver.(*txDriver)
	if !ok {
		panic("ent: EpisodeSeries is not a transactional entity")
	}
	es.config.driver = _tx.drv
	return es
}

// String implements the fmt.Stringer.
func (es *EpisodeSeries) String() string {
	var builder strings.Builder
	builder.WriteString("EpisodeSeries(")
	builder.WriteString(fmt.Sprintf("id=%v, ", es.ID))
	builder.WriteString("slug=")
	builder.WriteString(es.Slug)
	builder.WriteString(", ")
	builder.WriteString("title=")
	builder.WriteString(es.Title)
	builder.WriteString(", ")
	if v := es.Description; v != nil {
		builder.WriteString("description=")
		builder.WriteString(*v)
	}
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", es.Status))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(es.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(es.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// EpisodeSeriesSlice is a parsable slice of EpisodeSeries.
type EpisodeSeriesSlice []*EpisodeSeries
