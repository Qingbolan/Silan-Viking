// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"silan-backend/internal/ent/contentinteraction"
	"silan-backend/internal/ent/predicate"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ContentInteractionQuery is the builder for querying ContentInteraction entities.
type ContentInteractionQuery struct {
	config
	ctx        *QueryContext
	order      []contentinteraction.OrderOption
	inters     []Interceptor
	predicates []predicate.ContentInteraction
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ContentInteractionQuery builder.
func (ciq *ContentInteractionQuery) Where(ps ...predicate.ContentInteraction) *ContentInteractionQuery {
	ciq.predicates = append(ciq.predicates, ps...)
	return ciq
}

// Limit the number of records to be returned by this query.
func (ciq *ContentInteractionQuery) Limit(limit int) *ContentInteractionQuery {
	ciq.ctx.Limit = &limit
	return ciq
}

// Offset to start from.
func (ciq *ContentInteractionQuery) Offset(offset int) *ContentInteractionQuery {
	ciq.ctx.Offset = &offset
	return ciq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (ciq *ContentInteractionQuery) Unique(unique bool) *ContentInteractionQuery {
	ciq.ctx.Unique = &unique
	return ciq
}

// Order specifies how the records should be ordered.
func (ciq *ContentInteractionQuery) Order(o ...contentinteraction.OrderOption) *ContentInteractionQuery {
	ciq.order = append(ciq.order, o...)
	return ciq
}

// First returns the first ContentInteraction entity from the query.
// Returns a *NotFoundError when no ContentInteraction was found.
func (ciq *ContentInteractionQuery) First(ctx context.Context) (*ContentInteraction, error) {
	nodes, err := ciq.Limit(1).All(setContextOp(ctx, ciq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{contentinteraction.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (ciq *ContentInteractionQuery) FirstX(ctx context.Context) *ContentInteraction {
	node, err := ciq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ContentInteraction ID from the query.
// Returns a *NotFoundError when no ContentInteraction ID was found.
func (ciq *ContentInteractionQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = ciq.Limit(1).IDs(setContextOp(ctx, ciq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{contentinteraction.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (ciq *ContentInteractionQuery) FirstIDX(ctx context.Context) string {
	id, err := ciq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ContentInteraction entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ContentInteraction entity is found.
// Returns a *NotFoundError when no ContentInteraction entities are found.
func (ciq *ContentInteractionQuery) Only(ctx context.Context) (*ContentInteraction, error) {
	nodes, err := ciq.Limit(2).All(setContextOp(ctx, ciq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{contentinteraction.Label}
	default:
		return nil, &NotSingularError{contentinteraction.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (ciq *ContentInteractionQuery) OnlyX(ctx context.Context) *ContentInteraction {
	node, err := ciq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ContentInteraction ID in the query.
// Returns a *NotSingularError when more than one ContentInteraction ID is found.
// Returns a *NotFoundError when no entities are found.
func (ciq *ContentInteractionQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = ciq.Limit(2).IDs(setContextOp(ctx, ciq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{contentinteraction.Label}
	default:
		err = &NotSingularError{contentinteraction.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (ciq *ContentInteractionQuery) OnlyIDX(ctx context.Context) string {
	id, err := ciq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ContentInteractions.
func (ciq *ContentInteractionQuery) All(ctx context.Context) ([]*ContentInteraction, error) {
	ctx = setContextOp(ctx, ciq.ctx, ent.OpQueryAll)
	if err := ciq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ContentInteraction, *ContentInteractionQuery]()
	return withInterceptors[[]*ContentInteraction](ctx, ciq, qr, ciq.inters)
}

// AllX is like All, but panics if an error occurs.
func (ciq *ContentInteractionQuery) AllX(ctx context.Context) []*ContentInteraction {
	nodes, err := ciq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ContentInteraction IDs.
func (ciq *ContentInteractionQuery) IDs(ctx context.Context) (ids []string, err error) {
	if ciq.ctx.Unique == nil && ciq.path != nil {
		ciq.Unique(true)
	}
	ctx = setContextOp(ctx, ciq.ctx, ent.OpQueryIDs)
	if err = ciq.Select(contentinteraction.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (ciq *ContentInteractionQuery) IDsX(ctx context.Context) []string {
	ids, err := ciq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (ciq *ContentInteractionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, ciq.ctx, ent.OpQueryCount)
	if err := ciq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, ciq, querierCount[*ContentInteractionQuery](), ciq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (ciq *ContentInteractionQuery) CountX(ctx context.Context) int {
	count, err := ciq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (ciq *ContentInteractionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, ciq.ctx, ent.OpQueryExist)
	switch _, err := ciq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (ciq *ContentInteractionQuery) ExistX(ctx context.Context) bool {
	exist, err := ciq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ContentInteractionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (ciq *ContentInteractionQuery) Clone() *ContentInteractionQuery {
	if ciq == nil {
		return nil
	}
	return &ContentInteractionQuery{
		config:     ciq.config,
		ctx:        ciq.ctx.Clone(),
		order:      append([]contentinteraction.OrderOption{}, ciq.order...),
		inters:     append([]Interceptor{}, ciq.inters...),
		predicates: append([]predicate.ContentInteraction{}, ciq.predicates...),
		// clone intermediate query.
		sql:  ciq.sql.Clone(),
		path: ciq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		EntityType contentinteraction.EntityType `json:"entity_type,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ContentInteraction.Query().
//		GroupBy(contentinteraction.FieldEntityType).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (ciq *ContentInteractionQuery) GroupBy(field string, fields ...string) *ContentInteractionGroupBy {
	ciq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ContentInteractionGroupBy{build: ciq}
	grbuild.flds = &ciq.ctx.Fields
	grbuild.label = contentinteraction.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		EntityType contentinteraction.EntityType `json:"entity_type,omitempty"`
//	}
//
//	client.ContentInteraction.Query().
//		Select(contentinteraction.FieldEntityType).
//		Scan(ctx, &v)
func (ciq *ContentInteractionQuery) Select(fields ...string) *ContentInteractionSelect {
	ciq.ctx.Fields = append(ciq.ctx.Fields, fields...)
	sbuild := &ContentInteractionSelect{ContentInteractionQuery: ciq}
	sbuild.label = contentinteraction.Label
	sbuild.flds, sbuild.scan = &ciq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ContentInteractionSelect configured with the given aggregations.
func (ciq *ContentInteractionQuery) Aggregate(fns ...AggregateFunc) *ContentInteractionSelect {
	return ciq.Select().Aggregate(fns...)
}

func (ciq *ContentInteractionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range ciq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, ciq); err != nil {
				return err
			}
		}
	}
	for _, f := range ciq.ctx.Fields {
		if !contentinteraction.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if ciq.path != nil {
		prev, err := ciq.path(ctx)
		if err != nil {
			return err
		}
		ciq.sql = prev
	}
	return nil
}

func (ciq *ContentInteractionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ContentInteraction, error) {
	var (
		nodes = []*ContentInteraction{}
		_spec = ciq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ContentInteraction).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ContentInteraction{config: ciq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, ciq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (ciq *ContentInteractionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := ciq.querySpec()
	_spec.Node.Columns = ciq.ctx.Fields
	if len(ciq.ctx.Fields) > 0 {
		_spec.Unique = ciq.ctx.Unique != nil && *ciq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, ciq.driver, _spec)
}

func (ciq *ContentInteractionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(contentinteraction.Table, contentinteraction.Columns, sqlgraph.NewFieldSpec(contentinteraction.FieldID, field.TypeString))
	_spec.From = ciq.sql
	if unique := ciq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if ciq.path != nil {
		_spec.Unique = true
	}
	if fields := ciq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, contentinteraction.FieldID)
		for i := range fields {
			if fields[i] != contentinteraction.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := ciq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := ciq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := ciq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := ciq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (ciq *ContentInteractionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(ciq.driver.Dialect())
	t1 := builder.Table(contentinteraction.Table)
	columns := ciq.ctx.Fields
	if len(columns) == 0 {
		columns = contentinteraction.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if ciq.sql != nil {
		selector = ciq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if ciq.ctx.Unique != nil && *ciq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range ciq.predicates {
		p(selector)
	}
	for _, p := range ciq.order {
		p(selector)
	}
	if offset := ciq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := ciq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ContentInteractionGroupBy is the group-by builder for ContentInteraction entities.
type ContentInteractionGroupBy struct {
	selector
	build *ContentInteractionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cigb *ContentInteractionGroupBy) Aggregate(fns ...AggregateFunc) *ContentInteractionGroupBy {
	cigb.fns = append(cigb.fns, fns...)
	return cigb
}

// Scan applies the selector query and scans the result into the given value.
func (cigb *ContentInteractionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cigb.build.ctx, ent.OpQueryGroupBy)
	if err := cigb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ContentInteractionQuery, *ContentInteractionGroupBy](ctx, cigb.build, cigb, cigb.build.inters, v)
}

func (cigb *ContentInteractionGroupBy) sqlScan(ctx context.Context, root *ContentInteractionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(cigb.fns))
	for _, fn := range cigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*cigb.flds)+len(cigb.fns))
		for _, f := range *cigb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*cigb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cigb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ContentInteractionSelect is the builder for selecting fields of ContentInteraction entities.
type ContentInteractionSelect struct {
	*ContentInteractionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cis *ContentInteractionSelect) Aggregate(fns ...AggregateFunc) *ContentInteractionSelect {
	cis.fns = append(cis.fns, fns...)
	return cis
}

// Scan applies the selector query and scans the result into the given value.
func (cis *ContentInteractionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cis.ctx, ent.OpQuerySelect)
	if err := cis.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ContentInteractionQuery, *ContentInteractionSelect](ctx, cis.ContentInteractionQuery, cis, cis.inters, v)
}

func (cis *ContentInteractionSelect) sqlScan(ctx context.Context, root *ContentInteractionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cis.fns))
	for _, fn := range cis.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cis.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
