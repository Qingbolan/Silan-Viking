// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"silan-backend/internal/ent/itempart"
	"silan-backend/internal/ent/itemparttranslation"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// ItemPartTranslation is the model entity for the ItemPartTranslation schema.
type ItemPartTranslation struct {
	config `json:"-"`
	// ID of the ent.
	ID string `json:"id,omitempty"`
	// ItemPartID holds the value of the "item_part_id" field.
	ItemPartID string `json:"item_part_id,omitempty"`
	// LanguageCode holds the value of the "language_code" field.
	LanguageCode string `json:"language_code,omitempty"`
	// Body holds the value of the "body" field.
	Body string `json:"body,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ItemPartTranslationQuery when eager-loading is set.
	Edges        ItemPartTranslationEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ItemPartTranslationEdges holds the relations/edges for other nodes in the graph.
type ItemPartTranslationEdges struct {
	// ItemPart holds the value of the item_part edge.
	ItemPart *ItemPart `json:"item_part,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ItemPartOrErr returns the ItemPart value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ItemPartTranslationEdges) ItemPartOrErr() (*ItemPart, error) {
	if e.ItemPart != nil {
		return e.ItemPart, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: itempart.Label}
	}
	return nil, &NotLoadedError{edge: "item_part"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ItemPartTranslation) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case itemparttranslation.FieldID, itemparttranslation.FieldItemPartID, itemparttranslation.FieldLanguageCode, itemparttranslation.FieldBody:
			values[i] = new(sql.NullString)
		case itemparttranslation.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ItemPartTranslation fields.
func (ipt *ItemPartTranslation) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case itemparttranslation.FieldID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value.Valid {
				ipt.ID = value.String
			}
		case itemparttranslation.FieldItemPartID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field item_part_id", values[i])
			} else if value.Valid {
				ipt.ItemPartID = value.String
			}
		case itemparttranslation.FieldLanguageCode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field language_code", values[i])
			} else if value.Valid {
				ipt.LanguageCode = value.String
			}
		case itemparttranslation.FieldBody:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field body", values[i])
			} else if value.Valid {
				ipt.Body = value.String
			}
		case itemparttranslation.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				ipt.CreatedAt = value.Time
			}
		default:
			ipt.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ItemPartTranslation.
// This includes values selected through modifiers, order, etc.
func (ipt *ItemPartTranslation) Value(name string) (ent.Value, error) {
	return ipt.selectValues.Get(name)
}

// QueryItemPart queries the "item_part" edge of the ItemPartTranslation entity.
func (ipt *ItemPartTranslation) QueryItemPart() *ItemPartQuery {
	return NewItemPartTranslationClient(ipt.config).QueryItemPart(ipt)
}

// Update returns a builder for updating this ItemPartTranslation.
// Note that you need to call ItemPartTranslation.Unwrap() before calling this method if this ItemPartTranslation
// was returned from a transaction, and the transaction was committed or rolled back.
func (ipt *ItemPartTranslation) Update() *ItemPartTranslationUpdateOne {
	return NewItemPartTranslationClient(ipt.config).UpdateOne(ipt)
}

// Unwrap unwraps the ItemPartTranslation entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ipt *ItemPartTranslation) Unwrap() *ItemPartTranslation {
	_tx, ok := ipt.config.driver.(*txDriver)
	if !ok {
		panic("ent: ItemPartTranslation is not a transactional entity")
	}
	ipt.config.driver = _tx.drv
	return ipt
}

// String implements the fmt.Stringer.
func (ipt *ItemPartTranslation) String() string {
	var builder strings.Builder
	builder.WriteString("ItemPartTranslation(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ipt.ID))
	builder.WriteString("item_part_id=")
	builder.WriteString(ipt.ItemPartID)
	builder.WriteString(", ")
	builder.WriteString("language_code=")
	builder.WriteString(ipt.LanguageCode)
	builder.WriteString(", ")
	builder.WriteString("body=")
	builder.WriteString(ipt.Body)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(ipt.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ItemPartTranslations is a parsable slice of ItemPartTranslation.
type ItemPartTranslations []*ItemPartTranslation
