// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"silan-backend/internal/ent/contenttag"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// ContentTag is the model entity for the ContentTag schema.
type ContentTag struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// slug of the Tag entity (Tag.id)
	TagID string `json:"tag_id,omitempty"`
	// EntityType holds the value of the "entity_type" field.
	EntityType contenttag.EntityType `json:"entity_type,omitempty"`
	// stable id of the tagged Item
	EntityID string `json:"entity_id,omitempty"`
	// denormalised slug; lets list APIs resolve URLs without a second lookup
	EntitySlug   string `json:"entity_slug,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ContentTag) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case contenttag.FieldID:
			values[i] = new(sql.NullInt64)
		case contenttag.FieldTagID, contenttag.FieldEntityType, contenttag.FieldEntityID, contenttag.FieldEntitySlug:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ContentTag fields.
func (ct *ContentTag) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case contenttag.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ct.ID = int(value.Int64)
		case contenttag.FieldTagID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field tag_id", values[i])
			} else if value.Valid {
				ct.TagID = value.String
			}
		case contenttag.FieldEntityType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field entity_type", values[i])
			} else if value.Valid {
				ct.EntityType = contenttag.EntityType(value.String)
			}
		case contenttag.FieldEntityID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field entity_id", values[i])
			} else if value.Valid {
				ct.EntityID = value.String
			}
		case contenttag.FieldEntitySlug:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field entity_slug", values[i])
			} else if value.Valid {
				ct.EntitySlug = value.String
			}
		default:
			ct.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ContentTag.
// This includes values selected through modifiers, order, etc.
func (ct *ContentTag) Value(name string) (ent.Value, error) {
	return ct.selectValues.Get(name)
}

// Update returns a builder for updating this ContentTag.
// Note that you need to call ContentTag.Unwrap() before calling this method if this ContentTag
// was returned from a transaction, and the transaction was committed or rolled back.
func (ct *ContentTag) Update() *ContentTagUpdateOne {
	return NewContentTagClient(ct.config).UpdateOne(ct)
}

// Unwrap unwraps the ContentTag entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ct *ContentTag) Unwrap() *ContentTag {
	_tx, ok := ct.config.driver.(*txDriver)
	if !ok {
		panic("ent: ContentTag is not a transactional entity")
	}
	ct.config.driver = _tx.drv
	return ct
}

// String implements the fmt.Stringer.
func (ct *ContentTag) String() string {
	var builder strings.Builder
	builder.WriteString("ContentTag(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ct.ID))
	builder.WriteString("tag_id=")
	builder.WriteString(ct.TagID)
	builder.WriteString(", ")
	builder.WriteString("entity_type=")
	builder.WriteString(fmt.Sprintf("%v", ct.EntityType))
	builder.WriteString(", ")
	builder.WriteString("entity_id=")
	builder.WriteString(ct.EntityID)
	builder.WriteString(", ")
	builder.WriteString("entity_slug=")
	builder.WriteString(ct.EntitySlug)
	builder.WriteByte(')')
	return builder.String()
}

// ContentTags is a parsable slice of ContentTag.
type ContentTags []*ContentTag
