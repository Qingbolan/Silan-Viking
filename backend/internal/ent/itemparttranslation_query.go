// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"silan-backend/internal/ent/itempart"
	"silan-backend/internal/ent/itemparttranslation"
	"silan-backend/internal/ent/predicate"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// ItemPartTranslationQuery is the builder for querying ItemPartTranslation entities.
type ItemPartTranslationQuery struct {
	config
	ctx          *QueryContext
	order        []itemparttranslation.OrderOption
	inters       []Interceptor
	predicates   []predicate.ItemPartTranslation
	withItemPart *ItemPartQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the ItemPartTranslationQuery builder.
func (iptq *ItemPartTranslationQuery) Where(ps ...predicate.ItemPartTranslation) *ItemPartTranslationQuery {
	iptq.predicates = append(iptq.predicates, ps...)
	return iptq
}

// Limit the number of records to be returned by this query.
func (iptq *ItemPartTranslationQuery) Limit(limit int) *ItemPartTranslationQuery {
	iptq.ctx.Limit = &limit
	return iptq
}

// Offset to start from.
func (iptq *ItemPartTranslationQuery) Offset(offset int) *ItemPartTranslationQuery {
	iptq.ctx.Offset = &offset
	return iptq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (iptq *ItemPartTranslationQuery) Unique(unique bool) *ItemPartTranslationQuery {
	iptq.ctx.Unique = &unique
	return iptq
}

// Order specifies how the records should be ordered.
func (iptq *ItemPartTranslationQuery) Order(o ...itemparttranslation.OrderOption) *ItemPartTranslationQuery {
	iptq.order = append(iptq.order, o...)
	return iptq
}

// QueryItemPart chains the current query on the "item_part" edge.
func (iptq *ItemPartTranslationQuery) QueryItemPart() *ItemPartQuery {
	query := (&ItemPartClient{config: iptq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := iptq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := iptq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(itemparttranslation.Table, itemparttranslation.FieldID, selector),
			sqlgraph.To(itempart.Table, itempart.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, itemparttranslation.ItemPartTable, itemparttranslation.ItemPartColumn),
		)
		fromU = sqlgraph.SetNeighbors(iptq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first ItemPartTranslation entity from the query.
// Returns a *NotFoundError when no ItemPartTranslation was found.
func (iptq *ItemPartTranslationQuery) First(ctx context.Context) (*ItemPartTranslation, error) {
	nodes, err := iptq.Limit(1).All(setContextOp(ctx, iptq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{itemparttranslation.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (iptq *ItemPartTranslationQuery) FirstX(ctx context.Context) *ItemPartTranslation {
	node, err := iptq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first ItemPartTranslation ID from the query.
// Returns a *NotFoundError when no ItemPartTranslation ID was found.
func (iptq *ItemPartTranslationQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = iptq.Limit(1).IDs(setContextOp(ctx, iptq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{itemparttranslation.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (iptq *ItemPartTranslationQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := iptq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single ItemPartTranslation entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one ItemPartTranslation entity is found.
// Returns a *NotFoundError when no ItemPartTranslation entities are found.
func (iptq *ItemPartTranslationQuery) Only(ctx context.Context) (*ItemPartTranslation, error) {
	nodes, err := iptq.Limit(2).All(setContextOp(ctx, iptq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{itemparttranslation.Label}
	default:
		return nil, &NotSingularError{itemparttranslation.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (iptq *ItemPartTranslationQuery) OnlyX(ctx context.Context) *ItemPartTranslation {
	node, err := iptq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only ItemPartTranslation ID in the query.
// Returns a *NotSingularError when more than one ItemPartTranslation ID is found.
// Returns a *NotFoundError when no entities are found.
func (iptq *ItemPartTranslationQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = iptq.Limit(2).IDs(setContextOp(ctx, iptq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{itemparttranslation.Label}
	default:
		err = &NotSingularError{itemparttranslation.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (iptq *ItemPartTranslationQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := iptq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of ItemPartTranslations.
func (iptq *ItemPartTranslationQuery) All(ctx context.Context) ([]*ItemPartTranslation, error) {
	ctx = setContextOp(ctx, iptq.ctx, ent.OpQueryAll)
	if err := iptq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*ItemPartTranslation, *ItemPartTranslationQuery]()
	return withInterceptors[[]*ItemPartTranslation](ctx, iptq, qr, iptq.inters)
}

// AllX is like All, but panics if an error occurs.
func (iptq *ItemPartTranslationQuery) AllX(ctx context.Context) []*ItemPartTranslation {
	nodes, err := iptq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of ItemPartTranslation IDs.
func (iptq *ItemPartTranslationQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if iptq.ctx.Unique == nil && iptq.path != nil {
		iptq.Unique(true)
	}
	ctx = setContextOp(ctx, iptq.ctx, ent.OpQueryIDs)
	if err = iptq.Select(itemparttranslation.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (iptq *ItemPartTranslationQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := iptq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (iptq *ItemPartTranslationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, iptq.ctx, ent.OpQueryCount)
	if err := iptq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, iptq, querierCount[*ItemPartTranslationQuery](), iptq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (iptq *ItemPartTranslationQuery) CountX(ctx context.Context) int {
	count, err := iptq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (iptq *ItemPartTranslationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, iptq.ctx, ent.OpQueryExist)
	switch _, err := iptq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (iptq *ItemPartTranslationQuery) ExistX(ctx context.Context) bool {
	exist, err := iptq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the ItemPartTranslationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (iptq *ItemPartTranslationQuery) Clone() *ItemPartTranslationQuery {
	if iptq == nil {
		return nil
	}
	return &ItemPartTranslationQuery{
		config:       iptq.config,
		ctx:          iptq.ctx.Clone(),
		order:        append([]itemparttranslation.OrderOption{}, iptq.order...),
		inters:       append([]Interceptor{}, iptq.inters...),
		predicates:   append([]predicate.ItemPartTranslation{}, iptq.predicates...),
		withItemPart: iptq.withItemPart.Clone(),
		// clone intermediate query.
		sql:  iptq.sql.Clone(),
		path: iptq.path,
	}
}

// WithItemPart tells the query-builder to eager-load the nodes that are connected to
// the "item_part" edge. The optional arguments are used to configure the query builder of the edge.
func (iptq *ItemPartTranslationQuery) WithItemPart(opts ...func(*ItemPartQuery)) *ItemPartTranslationQuery {
	query := (&ItemPartClient{config: iptq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	iptq.withItemPart = query
	return iptq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		ItemPartID uuid.UUID `json:"item_part_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.ItemPartTranslation.Query().
//		GroupBy(itemparttranslation.FieldItemPartID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (iptq *ItemPartTranslationQuery) GroupBy(field string, fields ...string) *ItemPartTranslationGroupBy {
	iptq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &ItemPartTranslationGroupBy{build: iptq}
	grbuild.flds = &iptq.ctx.Fields
	grbuild.label = itemparttranslation.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		ItemPartID uuid.UUID `json:"item_part_id,omitempty"`
//	}
//
//	client.ItemPartTranslation.Query().
//		Select(itemparttranslation.FieldItemPartID).
//		Scan(ctx, &v)
func (iptq *ItemPartTranslationQuery) Select(fields ...string) *ItemPartTranslationSelect {
	iptq.ctx.Fields = append(iptq.ctx.Fields, fields...)
	sbuild := &ItemPartTranslationSelect{ItemPartTranslationQuery: iptq}
	sbuild.label = itemparttranslation.Label
	sbuild.flds, sbuild.scan = &iptq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a ItemPartTranslationSelect configured with the given aggregations.
func (iptq *ItemPartTranslationQuery) Aggregate(fns ...AggregateFunc) *ItemPartTranslationSelect {
	return iptq.Select().Aggregate(fns...)
}

func (iptq *ItemPartTranslationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range iptq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, iptq); err != nil {
				return err
			}
		}
	}
	for _, f := range iptq.ctx.Fields {
		if !itemparttranslation.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if iptq.path != nil {
		prev, err := iptq.path(ctx)
		if err != nil {
			return err
		}
		iptq.sql = prev
	}
	return nil
}

func (iptq *ItemPartTranslationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*ItemPartTranslation, error) {
	var (
		nodes       = []*ItemPartTranslation{}
		_spec       = iptq.querySpec()
		loadedTypes = [1]bool{
			iptq.withItemPart != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*ItemPartTranslation).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &ItemPartTranslation{config: iptq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, iptq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := iptq.withItemPart; query != nil {
		if err := iptq.loadItemPart(ctx, query, nodes, nil,
			func(n *ItemPartTranslation, e *ItemPart) { n.Edges.ItemPart = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (iptq *ItemPartTranslationQuery) loadItemPart(ctx context.Context, query *ItemPartQuery, nodes []*ItemPartTranslation, init func(*ItemPartTranslation), assign func(*ItemPartTranslation, *ItemPart)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*ItemPartTranslation)
	for i := range nodes {
		fk := nodes[i].ItemPartID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(itempart.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "item_part_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (iptq *ItemPartTranslationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := iptq.querySpec()
	_spec.Node.Columns = iptq.ctx.Fields
	if len(iptq.ctx.Fields) > 0 {
		_spec.Unique = iptq.ctx.Unique != nil && *iptq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, iptq.driver, _spec)
}

func (iptq *ItemPartTranslationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(itemparttranslation.Table, itemparttranslation.Columns, sqlgraph.NewFieldSpec(itemparttranslation.FieldID, field.TypeUUID))
	_spec.From = iptq.sql
	if unique := iptq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if iptq.path != nil {
		_spec.Unique = true
	}
	if fields := iptq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, itemparttranslation.FieldID)
		for i := range fields {
			if fields[i] != itemparttranslation.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if iptq.withItemPart != nil {
			_spec.Node.AddColumnOnce(itemparttranslation.FieldItemPartID)
		}
	}
	if ps := iptq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := iptq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := iptq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := iptq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (iptq *ItemPartTranslationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(iptq.driver.Dialect())
	t1 := builder.Table(itemparttranslation.Table)
	columns := iptq.ctx.Fields
	if len(columns) == 0 {
		columns = itemparttranslation.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if iptq.sql != nil {
		selector = iptq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if iptq.ctx.Unique != nil && *iptq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range iptq.predicates {
		p(selector)
	}
	for _, p := range iptq.order {
		p(selector)
	}
	if offset := iptq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := iptq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ItemPartTranslationGroupBy is the group-by builder for ItemPartTranslation entities.
type ItemPartTranslationGroupBy struct {
	selector
	build *ItemPartTranslationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (iptgb *ItemPartTranslationGroupBy) Aggregate(fns ...AggregateFunc) *ItemPartTranslationGroupBy {
	iptgb.fns = append(iptgb.fns, fns...)
	return iptgb
}

// Scan applies the selector query and scans the result into the given value.
func (iptgb *ItemPartTranslationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, iptgb.build.ctx, ent.OpQueryGroupBy)
	if err := iptgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ItemPartTranslationQuery, *ItemPartTranslationGroupBy](ctx, iptgb.build, iptgb, iptgb.build.inters, v)
}

func (iptgb *ItemPartTranslationGroupBy) sqlScan(ctx context.Context, root *ItemPartTranslationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(iptgb.fns))
	for _, fn := range iptgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*iptgb.flds)+len(iptgb.fns))
		for _, f := range *iptgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*iptgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := iptgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// ItemPartTranslationSelect is the builder for selecting fields of ItemPartTranslation entities.
type ItemPartTranslationSelect struct {
	*ItemPartTranslationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ipts *ItemPartTranslationSelect) Aggregate(fns ...AggregateFunc) *ItemPartTranslationSelect {
	ipts.fns = append(ipts.fns, fns...)
	return ipts
}

// Scan applies the selector query and scans the result into the given value.
func (ipts *ItemPartTranslationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ipts.ctx, ent.OpQuerySelect)
	if err := ipts.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*ItemPartTranslationQuery, *ItemPartTranslationSelect](ctx, ipts.ItemPartTranslationQuery, ipts, ipts.inters, v)
}

func (ipts *ItemPartTranslationSelect) sqlScan(ctx context.Context, root *ItemPartTranslationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ipts.fns))
	for _, fn := range ipts.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ipts.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ipts.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
