// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"silan-backend/internal/ent/contenttag"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ContentTagCreate is the builder for creating a ContentTag entity.
type ContentTagCreate struct {
	config
	mutation *ContentTagMutation
	hooks    []Hook
}

// SetTagID sets the "tag_id" field.
func (ctc *ContentTagCreate) SetTagID(s string) *ContentTagCreate {
	ctc.mutation.SetTagID(s)
	return ctc
}

// SetEntityType sets the "entity_type" field.
func (ctc *ContentTagCreate) SetEntityType(ct contenttag.EntityType) *ContentTagCreate {
	ctc.mutation.SetEntityType(ct)
	return ctc
}

// SetEntityID sets the "entity_id" field.
func (ctc *ContentTagCreate) SetEntityID(s string) *ContentTagCreate {
	ctc.mutation.SetEntityID(s)
	return ctc
}

// SetEntitySlug sets the "entity_slug" field.
func (ctc *ContentTagCreate) SetEntitySlug(s string) *ContentTagCreate {
	ctc.mutation.SetEntitySlug(s)
	return ctc
}

// Mutation returns the ContentTagMutation object of the builder.
func (ctc *ContentTagCreate) Mutation() *ContentTagMutation {
	return ctc.mutation
}

// Save creates the ContentTag in the database.
func (ctc *ContentTagCreate) Save(ctx context.Context) (*ContentTag, error) {
	return withHooks(ctx, ctc.sqlSave, ctc.mutation, ctc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (ctc *ContentTagCreate) SaveX(ctx context.Context) *ContentTag {
	v, err := ctc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (ctc *ContentTagCreate) Exec(ctx context.Context) error {
	_, err := ctc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ctc *ContentTagCreate) ExecX(ctx context.Context) {
	if err := ctc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (ctc *ContentTagCreate) check() error {
	if _, ok := ctc.mutation.TagID(); !ok {
		return &ValidationError{Name: "tag_id", err: errors.New(`ent: missing required field "ContentTag.tag_id"`)}
	}
	if _, ok := ctc.mutation.EntityType(); !ok {
		return &ValidationError{Name: "entity_type", err: errors.New(`ent: missing required field "ContentTag.entity_type"`)}
	}
	if v, ok := ctc.mutation.EntityType(); ok {
		if err := contenttag.EntityTypeValidator(v); err != nil {
			return &ValidationError{Name: "entity_type", err: fmt.Errorf(`ent: validator failed for field "ContentTag.entity_type": %w`, err)}
		}
	}
	if _, ok := ctc.mutation.EntityID(); !ok {
		return &ValidationError{Name: "entity_id", err: errors.New(`ent: missing required field "ContentTag.entity_id"`)}
	}
	if _, ok := ctc.mutation.EntitySlug(); !ok {
		return &ValidationError{Name: "entity_slug", err: errors.New(`ent: missing required field "ContentTag.entity_slug"`)}
	}
	return nil
}

func (ctc *ContentTagCreate) sqlSave(ctx context.Context) (*ContentTag, error) {
	if err := ctc.check(); err != nil {
		return nil, err
	}
	_node, _spec := ctc.createSpec()
	if err := sqlgraph.CreateNode(ctx, ctc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	ctc.mutation.id = &_node.ID
	ctc.mutation.done = true
	return _node, nil
}

func (ctc *ContentTagCreate) createSpec() (*ContentTag, *sqlgraph.CreateSpec) {
	var (
		_node = &ContentTag{config: ctc.config}
		_spec = sqlgraph.NewCreateSpec(contenttag.Table, sqlgraph.NewFieldSpec(contenttag.FieldID, field.TypeInt))
	)
	if value, ok := ctc.mutation.TagID(); ok {
		_spec.SetField(contenttag.FieldTagID, field.TypeString, value)
		_node.TagID = value
	}
	if value, ok := ctc.mutation.EntityType(); ok {
		_spec.SetField(contenttag.FieldEntityType, field.TypeEnum, value)
		_node.EntityType = value
	}
	if value, ok := ctc.mutation.EntityID(); ok {
		_spec.SetField(contenttag.FieldEntityID, field.TypeString, value)
		_node.EntityID = value
	}
	if value, ok := ctc.mutation.EntitySlug(); ok {
		_spec.SetField(contenttag.FieldEntitySlug, field.TypeString, value)
		_node.EntitySlug = value
	}
	return _node, _spec
}

// ContentTagCreateBulk is the builder for creating many ContentTag entities in bulk.
type ContentTagCreateBulk struct {
	config
	err      error
	builders []*ContentTagCreate
}

// Save creates the ContentTag entities in the database.
func (ctcb *ContentTagCreateBulk) Save(ctx context.Context) ([]*ContentTag, error) {
	if ctcb.err != nil {
		return nil, ctcb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(ctcb.builders))
	nodes := make([]*ContentTag, len(ctcb.builders))
	mutators := make([]Mutator, len(ctcb.builders))
	for i := range ctcb.builders {
		func(i int, root context.Context) {
			builder := ctcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*ContentTagMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, ctcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, ctcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, ctcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (ctcb *ContentTagCreateBulk) SaveX(ctx context.Context) []*ContentTag {
	v, err := ctcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (ctcb *ContentTagCreateBulk) Exec(ctx context.Context) error {
	_, err := ctcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ctcb *ContentTagCreateBulk) ExecX(ctx context.Context) {
	if err := ctcb.Exec(ctx); err != nil {
		panic(err)
	}
}
