// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"silan-backend/internal/ent/episodeseries"
	"silan-backend/internal/ent/episodeseriestranslation"
	"silan-backend/internal/ent/predicate"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// EpisodeSeriesTranslationQuery is the builder for querying EpisodeSeriesTranslation entities.
type EpisodeSeriesTranslationQuery struct {
	config
	ctx               *QueryContext
	order             []episodeseriestranslation.OrderOption
	inters            []Interceptor
	predicates        []predicate.EpisodeSeriesTranslation
	withEpisodeSeries *EpisodeSeriesQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EpisodeSeriesTranslationQuery builder.
func (estq *EpisodeSeriesTranslationQuery) Where(ps ...predicate.EpisodeSeriesTranslation) *EpisodeSeriesTranslationQuery {
	estq.predicates = append(estq.predicates, ps...)
	return estq
}

// Limit the number of records to be returned by this query.
func (estq *EpisodeSeriesTranslationQuery) Limit(limit int) *EpisodeSeriesTranslationQuery {
	estq.ctx.Limit = &limit
	return estq
}

// Offset to start from.
func (estq *EpisodeSeriesTranslationQuery) Offset(offset int) *EpisodeSeriesTranslationQuery {
	estq.ctx.Offset = &offset
	return estq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (estq *EpisodeSeriesTranslationQuery) Unique(unique bool) *EpisodeSeriesTranslationQuery {
	estq.ctx.Unique = &unique
	return estq
}

// Order specifies how the records should be ordered.
func (estq *EpisodeSeriesTranslationQuery) Order(o ...episodeseriestranslation.OrderOption) *EpisodeSeriesTranslationQuery {
	estq.order = append(estq.order, o...)
	return estq
}

// QueryEpisodeSeries chains the current query on the "episode_series" edge.
func (estq *EpisodeSeriesTranslationQuery) QueryEpisodeSeries() *EpisodeSeriesQuery {
	query := (&EpisodeSeriesClient{config: estq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := estq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := estq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(episodeseriestranslation.Table, episodeseriestranslation.FieldID, selector),
			sqlgraph.To(episodeseries.Table, episodeseries.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, episodeseriestranslation.EpisodeSeriesTable, episodeseriestranslation.EpisodeSeriesColumn),
		)
		fromU = sqlgraph.SetNeighbors(estq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first EpisodeSeriesTranslation entity from the query.
// Returns a *NotFoundError when no EpisodeSeriesTranslation was found.
func (estq *EpisodeSeriesTranslationQuery) First(ctx context.Context) (*EpisodeSeriesTranslation, error) {
	nodes, err := estq.Limit(1).All(setContextOp(ctx, estq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{episodeseriestranslation.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (estq *EpisodeSeriesTranslationQuery) FirstX(ctx context.Context) *EpisodeSeriesTranslation {
	node, err := estq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first EpisodeSeriesTranslation ID from the query.
// Returns a *NotFoundError when no EpisodeSeriesTranslation ID was found.
func (estq *EpisodeSeriesTranslationQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = estq.Limit(1).IDs(setContextOp(ctx, estq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{episodeseriestranslation.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (estq *EpisodeSeriesTranslationQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := estq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single EpisodeSeriesTranslation entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one EpisodeSeriesTranslation entity is found.
// Returns a *NotFoundError when no EpisodeSeriesTranslation entities are found.
func (estq *EpisodeSeriesTranslationQuery) Only(ctx context.Context) (*EpisodeSeriesTranslation, error) {
	nodes, err := estq.Limit(2).All(setContextOp(ctx, estq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{episodeseriestranslation.Label}
	default:
		return nil, &NotSingularError{episodeseriestranslation.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (estq *EpisodeSeriesTranslationQuery) OnlyX(ctx context.Context) *EpisodeSeriesTranslation {
	node, err := estq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only EpisodeSeriesTranslation ID in the query.
// Returns a *NotSingularError when more than one EpisodeSeriesTranslation ID is found.
// Returns a *NotFoundError when no entities are found.
func (estq *EpisodeSeriesTranslationQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = estq.Limit(2).IDs(setContextOp(ctx, estq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{episodeseriestranslation.Label}
	default:
		err = &NotSingularError{episodeseriestranslation.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (estq *EpisodeSeriesTranslationQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := estq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of EpisodeSeriesTranslations.
func (estq *EpisodeSeriesTranslationQuery) All(ctx context.Context) ([]*EpisodeSeriesTranslation, error) {
	ctx = setContextOp(ctx, estq.ctx, ent.OpQueryAll)
	if err := estq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*EpisodeSeriesTranslation, *EpisodeSeriesTranslationQuery]()
	return withInterceptors[[]*EpisodeSeriesTranslation](ctx, estq, qr, estq.inters)
}

// AllX is like All, but panics if an error occurs.
func (estq *EpisodeSeriesTranslationQuery) AllX(ctx context.Context) []*EpisodeSeriesTranslation {
	nodes, err := estq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of EpisodeSeriesTranslation IDs.
func (estq *EpisodeSeriesTranslationQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if estq.ctx.Unique == nil && estq.path != nil {
		estq.Unique(true)
	}
	ctx = setContextOp(ctx, estq.ctx, ent.OpQueryIDs)
	if err = estq.Select(episodeseriestranslation.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (estq *EpisodeSeriesTranslationQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := estq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (estq *EpisodeSeriesTranslationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, estq.ctx, ent.OpQueryCount)
	if err := estq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, estq, querierCount[*EpisodeSeriesTranslationQuery](), estq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (estq *EpisodeSeriesTranslationQuery) CountX(ctx context.Context) int {
	count, err := estq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (estq *EpisodeSeriesTranslationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, estq.ctx, ent.OpQueryExist)
	switch _, err := estq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (estq *EpisodeSeriesTranslationQuery) ExistX(ctx context.Context) bool {
	exist, err := estq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EpisodeSeriesTranslationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (estq *EpisodeSeriesTranslationQuery) Clone() *EpisodeSeriesTranslationQuery {
	if estq == nil {
		return nil
	}
	return &EpisodeSeriesTranslationQuery{
		config:            estq.config,
		ctx:               estq.ctx.Clone(),
		order:             append([]episodeseriestranslation.OrderOption{}, estq.order...),
		inters:            append([]Interceptor{}, estq.inters...),
		predicates:        append([]predicate.EpisodeSeriesTranslation{}, estq.predicates...),
		withEpisodeSeries: estq.withEpisodeSeries.Clone(),
		// clone intermediate query.
		sql:  estq.sql.Clone(),
		path: estq.path,
	}
}

// WithEpisodeSeries tells the query-builder to eager-load the nodes that are connected to
// the "episode_series" edge. The optional arguments are used to configure the query builder of the edge.
func (estq *EpisodeSeriesTranslationQuery) WithEpisodeSeries(opts ...func(*EpisodeSeriesQuery)) *EpisodeSeriesTranslationQuery {
	query := (&EpisodeSeriesClient{config: estq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	estq.withEpisodeSeries = query
	return estq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		EpisodeSeriesID uuid.UUID `json:"episode_series_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.EpisodeSeriesTranslation.Query().
//		GroupBy(episodeseriestranslation.FieldEpisodeSeriesID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (estq *EpisodeSeriesTranslationQuery) GroupBy(field string, fields ...string) *EpisodeSeriesTranslationGroupBy {
	estq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &EpisodeSeriesTranslationGroupBy{build: estq}
	grbuild.flds = &estq.ctx.Fields
	grbuild.label = episodeseriestranslation.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		EpisodeSeriesID uuid.UUID `json:"episode_series_id,omitempty"`
//	}
//
//	client.EpisodeSeriesTranslation.Query().
//		Select(episodeseriestranslation.FieldEpisodeSeriesID).
//		Scan(ctx, &v)
func (estq *EpisodeSeriesTranslationQuery) Select(fields ...string) *EpisodeSeriesTranslationSelect {
	estq.ctx.Fields = append(estq.ctx.Fields, fields...)
	sbuild := &EpisodeSeriesTranslationSelect{EpisodeSeriesTranslationQuery: estq}
	sbuild.label = episodeseriestranslation.Label
	sbuild.flds, sbuild.scan = &estq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a EpisodeSeriesTranslationSelect configured with the given aggregations.
func (estq *EpisodeSeriesTranslationQuery) Aggregate(fns ...AggregateFunc) *EpisodeSeriesTranslationSelect {
	return estq.Select().Aggregate(fns...)
}

func (estq *EpisodeSeriesTranslationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range estq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, estq); err != nil {
				return err
			}
		}
	}
	for _, f := range estq.ctx.Fields {
		if !episodeseriestranslation.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if estq.path != nil {
		prev, err := estq.path(ctx)
		if err != nil {
			return err
		}
		estq.sql = prev
	}
	return nil
}

func (estq *EpisodeSeriesTranslationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*EpisodeSeriesTranslation, error) {
	var (
		nodes       = []*EpisodeSeriesTranslation{}
		_spec       = estq.querySpec()
		loadedTypes = [1]bool{
			estq.withEpisodeSeries != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*EpisodeSeriesTranslation).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &EpisodeSeriesTranslation{config: estq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, estq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := estq.withEpisodeSeries; query != nil {
		if err := estq.loadEpisodeSeries(ctx, query, nodes, nil,
			func(n *EpisodeSeriesTranslation, e *EpisodeSeries) { n.Edges.EpisodeSeries = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (estq *EpisodeSeriesTranslationQuery) loadEpisodeSeries(ctx context.Context, query *EpisodeSeriesQuery, nodes []*EpisodeSeriesTranslation, init func(*EpisodeSeriesTranslation), assign func(*EpisodeSeriesTranslation, *EpisodeSeries)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*EpisodeSeriesTranslation)
	for i := range nodes {
		fk := nodes[i].EpisodeSeriesID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(episodeseries.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "episode_series_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (estq *EpisodeSeriesTranslationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := estq.querySpec()
	_spec.Node.Columns = estq.ctx.Fields
	if len(estq.ctx.Fields) > 0 {
		_spec.Unique = estq.ctx.Unique != nil && *estq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, estq.driver, _spec)
}

func (estq *EpisodeSeriesTranslationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(episodeseriestranslation.Table, episodeseriestranslation.Columns, sqlgraph.NewFieldSpec(episodeseriestranslation.FieldID, field.TypeUUID))
	_spec.From = estq.sql
	if unique := estq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if estq.path != nil {
		_spec.Unique = true
	}
	if fields := estq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, episodeseriestranslation.FieldID)
		for i := range fields {
			if fields[i] != episodeseriestranslation.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if estq.withEpisodeSeries != nil {
			_spec.Node.AddColumnOnce(episodeseriestranslation.FieldEpisodeSeriesID)
		}
	}
	if ps := estq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := estq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := estq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := estq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (estq *EpisodeSeriesTranslationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(estq.driver.Dialect())
	t1 := builder.Table(episodeseriestranslation.Table)
	columns := estq.ctx.Fields
	if len(columns) == 0 {
		columns = episodeseriestranslation.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if estq.sql != nil {
		selector = estq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if estq.ctx.Unique != nil && *estq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range estq.predicates {
		p(selector)
	}
	for _, p := range estq.order {
		p(selector)
	}
	if offset := estq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := estq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// EpisodeSeriesTranslationGroupBy is the group-by builder for EpisodeSeriesTranslation entities.
type EpisodeSeriesTranslationGroupBy struct {
	selector
	build *EpisodeSeriesTranslationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (estgb *EpisodeSeriesTranslationGroupBy) Aggregate(fns ...AggregateFunc) *EpisodeSeriesTranslationGroupBy {
	estgb.fns = append(estgb.fns, fns...)
	return estgb
}

// Scan applies the selector query and scans the result into the given value.
func (estgb *EpisodeSeriesTranslationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, estgb.build.ctx, ent.OpQueryGroupBy)
	if err := estgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EpisodeSeriesTranslationQuery, *EpisodeSeriesTranslationGroupBy](ctx, estgb.build, estgb, estgb.build.inters, v)
}

func (estgb *EpisodeSeriesTranslationGroupBy) sqlScan(ctx context.Context, root *EpisodeSeriesTranslationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(estgb.fns))
	for _, fn := range estgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*estgb.flds)+len(estgb.fns))
		for _, f := range *estgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*estgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := estgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// EpisodeSeriesTranslationSelect is the builder for selecting fields of EpisodeSeriesTranslation entities.
type EpisodeSeriesTranslationSelect struct {
	*EpisodeSeriesTranslationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ests *EpisodeSeriesTranslationSelect) Aggregate(fns ...AggregateFunc) *EpisodeSeriesTranslationSelect {
	ests.fns = append(ests.fns, fns...)
	return ests
}

// Scan applies the selector query and scans the result into the given value.
func (ests *EpisodeSeriesTranslationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ests.ctx, ent.OpQuerySelect)
	if err := ests.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EpisodeSeriesTranslationQuery, *EpisodeSeriesTranslationSelect](ctx, ests.EpisodeSeriesTranslationQuery, ests, ests.inters, v)
}

func (ests *EpisodeSeriesTranslationSelect) sqlScan(ctx context.Context, root *EpisodeSeriesTranslationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ests.fns))
	for _, fn := range ests.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ests.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ests.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
