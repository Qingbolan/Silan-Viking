// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"silan-backend/internal/ent/predicate"
	"silan-backend/internal/ent/statscachevisitor"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// StatsCacheVisitorQuery is the builder for querying StatsCacheVisitor entities.
type StatsCacheVisitorQuery struct {
	config
	ctx        *QueryContext
	order      []statscachevisitor.OrderOption
	inters     []Interceptor
	predicates []predicate.StatsCacheVisitor
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the StatsCacheVisitorQuery builder.
func (scvq *StatsCacheVisitorQuery) Where(ps ...predicate.StatsCacheVisitor) *StatsCacheVisitorQuery {
	scvq.predicates = append(scvq.predicates, ps...)
	return scvq
}

// Limit the number of records to be returned by this query.
func (scvq *StatsCacheVisitorQuery) Limit(limit int) *StatsCacheVisitorQuery {
	scvq.ctx.Limit = &limit
	return scvq
}

// Offset to start from.
func (scvq *StatsCacheVisitorQuery) Offset(offset int) *StatsCacheVisitorQuery {
	scvq.ctx.Offset = &offset
	return scvq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (scvq *StatsCacheVisitorQuery) Unique(unique bool) *StatsCacheVisitorQuery {
	scvq.ctx.Unique = &unique
	return scvq
}

// Order specifies how the records should be ordered.
func (scvq *StatsCacheVisitorQuery) Order(o ...statscachevisitor.OrderOption) *StatsCacheVisitorQuery {
	scvq.order = append(scvq.order, o...)
	return scvq
}

// First returns the first StatsCacheVisitor entity from the query.
// Returns a *NotFoundError when no StatsCacheVisitor was found.
func (scvq *StatsCacheVisitorQuery) First(ctx context.Context) (*StatsCacheVisitor, error) {
	nodes, err := scvq.Limit(1).All(setContextOp(ctx, scvq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{statscachevisitor.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (scvq *StatsCacheVisitorQuery) FirstX(ctx context.Context) *StatsCacheVisitor {
	node, err := scvq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first StatsCacheVisitor ID from the query.
// Returns a *NotFoundError when no StatsCacheVisitor ID was found.
func (scvq *StatsCacheVisitorQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = scvq.Limit(1).IDs(setContextOp(ctx, scvq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{statscachevisitor.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (scvq *StatsCacheVisitorQuery) FirstIDX(ctx context.Context) int {
	id, err := scvq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single StatsCacheVisitor entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one StatsCacheVisitor entity is found.
// Returns a *NotFoundError when no StatsCacheVisitor entities are found.
func (scvq *StatsCacheVisitorQuery) Only(ctx context.Context) (*StatsCacheVisitor, error) {
	nodes, err := scvq.Limit(2).All(setContextOp(ctx, scvq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{statscachevisitor.Label}
	default:
		return nil, &NotSingularError{statscachevisitor.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (scvq *StatsCacheVisitorQuery) OnlyX(ctx context.Context) *StatsCacheVisitor {
	node, err := scvq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only StatsCacheVisitor ID in the query.
// Returns a *NotSingularError when more than one StatsCacheVisitor ID is found.
// Returns a *NotFoundError when no entities are found.
func (scvq *StatsCacheVisitorQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = scvq.Limit(2).IDs(setContextOp(ctx, scvq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{statscachevisitor.Label}
	default:
		err = &NotSingularError{statscachevisitor.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (scvq *StatsCacheVisitorQuery) OnlyIDX(ctx context.Context) int {
	id, err := scvq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of StatsCacheVisitors.
func (scvq *StatsCacheVisitorQuery) All(ctx context.Context) ([]*StatsCacheVisitor, error) {
	ctx = setContextOp(ctx, scvq.ctx, ent.OpQueryAll)
	if err := scvq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*StatsCacheVisitor, *StatsCacheVisitorQuery]()
	return withInterceptors[[]*StatsCacheVisitor](ctx, scvq, qr, scvq.inters)
}

// AllX is like All, but panics if an error occurs.
func (scvq *StatsCacheVisitorQuery) AllX(ctx context.Context) []*StatsCacheVisitor {
	nodes, err := scvq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of StatsCacheVisitor IDs.
func (scvq *StatsCacheVisitorQuery) IDs(ctx context.Context) (ids []int, err error) {
	if scvq.ctx.Unique == nil && scvq.path != nil {
		scvq.Unique(true)
	}
	ctx = setContextOp(ctx, scvq.ctx, ent.OpQueryIDs)
	if err = scvq.Select(statscachevisitor.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (scvq *StatsCacheVisitorQuery) IDsX(ctx context.Context) []int {
	ids, err := scvq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (scvq *StatsCacheVisitorQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, scvq.ctx, ent.OpQueryCount)
	if err := scvq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, scvq, querierCount[*StatsCacheVisitorQuery](), scvq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (scvq *StatsCacheVisitorQuery) CountX(ctx context.Context) int {
	count, err := scvq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (scvq *StatsCacheVisitorQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, scvq.ctx, ent.OpQueryExist)
	switch _, err := scvq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (scvq *StatsCacheVisitorQuery) ExistX(ctx context.Context) bool {
	exist, err := scvq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the StatsCacheVisitorQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (scvq *StatsCacheVisitorQuery) Clone() *StatsCacheVisitorQuery {
	if scvq == nil {
		return nil
	}
	return &StatsCacheVisitorQuery{
		config:     scvq.config,
		ctx:        scvq.ctx.Clone(),
		order:      append([]statscachevisitor.OrderOption{}, scvq.order...),
		inters:     append([]Interceptor{}, scvq.inters...),
		predicates: append([]predicate.StatsCacheVisitor{}, scvq.predicates...),
		// clone intermediate query.
		sql:  scvq.sql.Clone(),
		path: scvq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		EntityType statscachevisitor.EntityType `json:"entity_type,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.StatsCacheVisitor.Query().
//		GroupBy(statscachevisitor.FieldEntityType).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (scvq *StatsCacheVisitorQuery) GroupBy(field string, fields ...string) *StatsCacheVisitorGroupBy {
	scvq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &StatsCacheVisitorGroupBy{build: scvq}
	grbuild.flds = &scvq.ctx.Fields
	grbuild.label = statscachevisitor.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		EntityType statscachevisitor.EntityType `json:"entity_type,omitempty"`
//	}
//
//	client.StatsCacheVisitor.Query().
//		Select(statscachevisitor.FieldEntityType).
//		Scan(ctx, &v)
func (scvq *StatsCacheVisitorQuery) Select(fields ...string) *StatsCacheVisitorSelect {
	scvq.ctx.Fields = append(scvq.ctx.Fields, fields...)
	sbuild := &StatsCacheVisitorSelect{StatsCacheVisitorQuery: scvq}
	sbuild.label = statscachevisitor.Label
	sbuild.flds, sbuild.scan = &scvq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a StatsCacheVisitorSelect configured with the given aggregations.
func (scvq *StatsCacheVisitorQuery) Aggregate(fns ...AggregateFunc) *StatsCacheVisitorSelect {
	return scvq.Select().Aggregate(fns...)
}

func (scvq *StatsCacheVisitorQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range scvq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, scvq); err != nil {
				return err
			}
		}
	}
	for _, f := range scvq.ctx.Fields {
		if !statscachevisitor.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if scvq.path != nil {
		prev, err := scvq.path(ctx)
		if err != nil {
			return err
		}
		scvq.sql = prev
	}
	return nil
}

func (scvq *StatsCacheVisitorQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*StatsCacheVisitor, error) {
	var (
		nodes = []*StatsCacheVisitor{}
		_spec = scvq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*StatsCacheVisitor).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &StatsCacheVisitor{config: scvq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, scvq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (scvq *StatsCacheVisitorQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := scvq.querySpec()
	_spec.Node.Columns = scvq.ctx.Fields
	if len(scvq.ctx.Fields) > 0 {
		_spec.Unique = scvq.ctx.Unique != nil && *scvq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, scvq.driver, _spec)
}

func (scvq *StatsCacheVisitorQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(statscachevisitor.Table, statscachevisitor.Columns, sqlgraph.NewFieldSpec(statscachevisitor.FieldID, field.TypeInt))
	_spec.From = scvq.sql
	if unique := scvq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if scvq.path != nil {
		_spec.Unique = true
	}
	if fields := scvq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, statscachevisitor.FieldID)
		for i := range fields {
			if fields[i] != statscachevisitor.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := scvq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := scvq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := scvq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := scvq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (scvq *StatsCacheVisitorQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(scvq.driver.Dialect())
	t1 := builder.Table(statscachevisitor.Table)
	columns := scvq.ctx.Fields
	if len(columns) == 0 {
		columns = statscachevisitor.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if scvq.sql != nil {
		selector = scvq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if scvq.ctx.Unique != nil && *scvq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range scvq.predicates {
		p(selector)
	}
	for _, p := range scvq.order {
		p(selector)
	}
	if offset := scvq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := scvq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// StatsCacheVisitorGroupBy is the group-by builder for StatsCacheVisitor entities.
type StatsCacheVisitorGroupBy struct {
	selector
	build *StatsCacheVisitorQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (scvgb *StatsCacheVisitorGroupBy) Aggregate(fns ...AggregateFunc) *StatsCacheVisitorGroupBy {
	scvgb.fns = append(scvgb.fns, fns...)
	return scvgb
}

// Scan applies the selector query and scans the result into the given value.
func (scvgb *StatsCacheVisitorGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, scvgb.build.ctx, ent.OpQueryGroupBy)
	if err := scvgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StatsCacheVisitorQuery, *StatsCacheVisitorGroupBy](ctx, scvgb.build, scvgb, scvgb.build.inters, v)
}

func (scvgb *StatsCacheVisitorGroupBy) sqlScan(ctx context.Context, root *StatsCacheVisitorQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(scvgb.fns))
	for _, fn := range scvgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*scvgb.flds)+len(scvgb.fns))
		for _, f := range *scvgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*scvgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := scvgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// StatsCacheVisitorSelect is the builder for selecting fields of StatsCacheVisitor entities.
type StatsCacheVisitorSelect struct {
	*StatsCacheVisitorQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (scvs *StatsCacheVisitorSelect) Aggregate(fns ...AggregateFunc) *StatsCacheVisitorSelect {
	scvs.fns = append(scvs.fns, fns...)
	return scvs
}

// Scan applies the selector query and scans the result into the given value.
func (scvs *StatsCacheVisitorSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, scvs.ctx, ent.OpQuerySelect)
	if err := scvs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StatsCacheVisitorQuery, *StatsCacheVisitorSelect](ctx, scvs.StatsCacheVisitorQuery, scvs, scvs.inters, v)
}

func (scvs *StatsCacheVisitorSelect) sqlScan(ctx context.Context, root *StatsCacheVisitorQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(scvs.fns))
	for _, fn := range scvs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*scvs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := scvs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
