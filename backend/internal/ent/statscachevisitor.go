// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"silan-backend/internal/ent/statscachevisitor"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// StatsCacheVisitor is the model entity for the StatsCacheVisitor schema.
type StatsCacheVisitor struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// EntityType holds the value of the "entity_type" field.
	EntityType statscachevisitor.EntityType `json:"entity_type,omitempty"`
	// EntityID holds the value of the "entity_id" field.
	EntityID string `json:"entity_id,omitempty"`
	// Fingerprint holds the value of the "fingerprint" field.
	Fingerprint string `json:"fingerprint,omitempty"`
	// anonymised IP, e.g. `203.0.113.0/24`; raw IP never leaves the server
	IPMasked string `json:"ip_masked,omitempty"`
	// VisitorKind holds the value of the "visitor_kind" field.
	VisitorKind statscachevisitor.VisitorKind `json:"visitor_kind,omitempty"`
	// ReferrerKind holds the value of the "referrer_kind" field.
	ReferrerKind statscachevisitor.ReferrerKind `json:"referrer_kind,omitempty"`
	// LastSeenAt holds the value of the "last_seen_at" field.
	LastSeenAt time.Time `json:"last_seen_at,omitempty"`
	// SyncedAt holds the value of the "synced_at" field.
	SyncedAt     time.Time `json:"synced_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*StatsCacheVisitor) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case statscachevisitor.FieldID:
			values[i] = new(sql.NullInt64)
		case statscachevisitor.FieldEntityType, statscachevisitor.FieldEntityID, statscachevisitor.FieldFingerprint, statscachevisitor.FieldIPMasked, statscachevisitor.FieldVisitorKind, statscachevisitor.FieldReferrerKind:
			values[i] = new(sql.NullString)
		case statscachevisitor.FieldLastSeenAt, statscachevisitor.FieldSyncedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the StatsCacheVisitor fields.
func (scv *StatsCacheVisitor) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case statscachevisitor.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			scv.ID = int(value.Int64)
		case statscachevisitor.FieldEntityType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field entity_type", values[i])
			} else if value.Valid {
				scv.EntityType = statscachevisitor.EntityType(value.String)
			}
		case statscachevisitor.FieldEntityID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field entity_id", values[i])
			} else if value.Valid {
				scv.EntityID = value.String
			}
		case statscachevisitor.FieldFingerprint:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field fingerprint", values[i])
			} else if value.Valid {
				scv.Fingerprint = value.String
			}
		case statscachevisitor.FieldIPMasked:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field ip_masked", values[i])
			} else if value.Valid {
				scv.IPMasked = value.String
			}
		case statscachevisitor.FieldVisitorKind:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field visitor_kind", values[i])
			} else if value.Valid {
				scv.VisitorKind = statscachevisitor.VisitorKind(value.String)
			}
		case statscachevisitor.FieldReferrerKind:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field referrer_kind", values[i])
			} else if value.Valid {
				scv.ReferrerKind = statscachevisitor.ReferrerKind(value.String)
			}
		case statscachevisitor.FieldLastSeenAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_seen_at", values[i])
			} else if value.Valid {
				scv.LastSeenAt = value.Time
			}
		case statscachevisitor.FieldSyncedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field synced_at", values[i])
			} else if value.Valid {
				scv.SyncedAt = value.Time
			}
		default:
			scv.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the StatsCacheVisitor.
// This includes values selected through modifiers, order, etc.
func (scv *StatsCacheVisitor) Value(name string) (ent.Value, error) {
	return scv.selectValues.Get(name)
}

// Update returns a builder for updating this StatsCacheVisitor.
// Note that you need to call StatsCacheVisitor.Unwrap() before calling this method if this StatsCacheVisitor
// was returned from a transaction, and the transaction was committed or rolled back.
func (scv *StatsCacheVisitor) Update() *StatsCacheVisitorUpdateOne {
	return NewStatsCacheVisitorClient(scv.config).UpdateOne(scv)
}

// Unwrap unwraps the StatsCacheVisitor entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (scv *StatsCacheVisitor) Unwrap() *StatsCacheVisitor {
	_tx, ok := scv.config.driver.(*txDriver)
	if !ok {
		panic("ent: StatsCacheVisitor is not a transactional entity")
	}
	scv.config.driver = _tx.drv
	return scv
}

// String implements the fmt.Stringer.
func (scv *StatsCacheVisitor) String() string {
	var builder strings.Builder
	builder.WriteString("StatsCacheVisitor(")
	builder.WriteString(fmt.Sprintf("id=%v, ", scv.ID))
	builder.WriteString("entity_type=")
	builder.WriteString(fmt.Sprintf("%v", scv.EntityType))
	builder.WriteString(", ")
	builder.WriteString("entity_id=")
	builder.WriteString(scv.EntityID)
	builder.WriteString(", ")
	builder.WriteString("fingerprint=")
	builder.WriteString(scv.Fingerprint)
	builder.WriteString(", ")
	builder.WriteString("ip_masked=")
	builder.WriteString(scv.IPMasked)
	builder.WriteString(", ")
	builder.WriteString("visitor_kind=")
	builder.WriteString(fmt.Sprintf("%v", scv.VisitorKind))
	builder.WriteString(", ")
	builder.WriteString("referrer_kind=")
	builder.WriteString(fmt.Sprintf("%v", scv.ReferrerKind))
	builder.WriteString(", ")
	builder.WriteString("last_seen_at=")
	builder.WriteString(scv.LastSeenAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("synced_at=")
	builder.WriteString(scv.SyncedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// StatsCacheVisitors is a parsable slice of StatsCacheVisitor.
type StatsCacheVisitors []*StatsCacheVisitor
