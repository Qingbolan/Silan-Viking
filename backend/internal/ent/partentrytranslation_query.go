// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"silan-backend/internal/ent/partentry"
	"silan-backend/internal/ent/partentrytranslation"
	"silan-backend/internal/ent/predicate"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PartEntryTranslationQuery is the builder for querying PartEntryTranslation entities.
type PartEntryTranslationQuery struct {
	config
	ctx           *QueryContext
	order         []partentrytranslation.OrderOption
	inters        []Interceptor
	predicates    []predicate.PartEntryTranslation
	withPartEntry *PartEntryQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PartEntryTranslationQuery builder.
func (petq *PartEntryTranslationQuery) Where(ps ...predicate.PartEntryTranslation) *PartEntryTranslationQuery {
	petq.predicates = append(petq.predicates, ps...)
	return petq
}

// Limit the number of records to be returned by this query.
func (petq *PartEntryTranslationQuery) Limit(limit int) *PartEntryTranslationQuery {
	petq.ctx.Limit = &limit
	return petq
}

// Offset to start from.
func (petq *PartEntryTranslationQuery) Offset(offset int) *PartEntryTranslationQuery {
	petq.ctx.Offset = &offset
	return petq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (petq *PartEntryTranslationQuery) Unique(unique bool) *PartEntryTranslationQuery {
	petq.ctx.Unique = &unique
	return petq
}

// Order specifies how the records should be ordered.
func (petq *PartEntryTranslationQuery) Order(o ...partentrytranslation.OrderOption) *PartEntryTranslationQuery {
	petq.order = append(petq.order, o...)
	return petq
}

// QueryPartEntry chains the current query on the "part_entry" edge.
func (petq *PartEntryTranslationQuery) QueryPartEntry() *PartEntryQuery {
	query := (&PartEntryClient{config: petq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := petq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := petq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(partentrytranslation.Table, partentrytranslation.FieldID, selector),
			sqlgraph.To(partentry.Table, partentry.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, partentrytranslation.PartEntryTable, partentrytranslation.PartEntryColumn),
		)
		fromU = sqlgraph.SetNeighbors(petq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PartEntryTranslation entity from the query.
// Returns a *NotFoundError when no PartEntryTranslation was found.
func (petq *PartEntryTranslationQuery) First(ctx context.Context) (*PartEntryTranslation, error) {
	nodes, err := petq.Limit(1).All(setContextOp(ctx, petq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{partentrytranslation.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (petq *PartEntryTranslationQuery) FirstX(ctx context.Context) *PartEntryTranslation {
	node, err := petq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PartEntryTranslation ID from the query.
// Returns a *NotFoundError when no PartEntryTranslation ID was found.
func (petq *PartEntryTranslationQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = petq.Limit(1).IDs(setContextOp(ctx, petq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{partentrytranslation.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (petq *PartEntryTranslationQuery) FirstIDX(ctx context.Context) string {
	id, err := petq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PartEntryTranslation entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PartEntryTranslation entity is found.
// Returns a *NotFoundError when no PartEntryTranslation entities are found.
func (petq *PartEntryTranslationQuery) Only(ctx context.Context) (*PartEntryTranslation, error) {
	nodes, err := petq.Limit(2).All(setContextOp(ctx, petq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{partentrytranslation.Label}
	default:
		return nil, &NotSingularError{partentrytranslation.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (petq *PartEntryTranslationQuery) OnlyX(ctx context.Context) *PartEntryTranslation {
	node, err := petq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PartEntryTranslation ID in the query.
// Returns a *NotSingularError when more than one PartEntryTranslation ID is found.
// Returns a *NotFoundError when no entities are found.
func (petq *PartEntryTranslationQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = petq.Limit(2).IDs(setContextOp(ctx, petq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{partentrytranslation.Label}
	default:
		err = &NotSingularError{partentrytranslation.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (petq *PartEntryTranslationQuery) OnlyIDX(ctx context.Context) string {
	id, err := petq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PartEntryTranslations.
func (petq *PartEntryTranslationQuery) All(ctx context.Context) ([]*PartEntryTranslation, error) {
	ctx = setContextOp(ctx, petq.ctx, ent.OpQueryAll)
	if err := petq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PartEntryTranslation, *PartEntryTranslationQuery]()
	return withInterceptors[[]*PartEntryTranslation](ctx, petq, qr, petq.inters)
}

// AllX is like All, but panics if an error occurs.
func (petq *PartEntryTranslationQuery) AllX(ctx context.Context) []*PartEntryTranslation {
	nodes, err := petq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PartEntryTranslation IDs.
func (petq *PartEntryTranslationQuery) IDs(ctx context.Context) (ids []string, err error) {
	if petq.ctx.Unique == nil && petq.path != nil {
		petq.Unique(true)
	}
	ctx = setContextOp(ctx, petq.ctx, ent.OpQueryIDs)
	if err = petq.Select(partentrytranslation.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (petq *PartEntryTranslationQuery) IDsX(ctx context.Context) []string {
	ids, err := petq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (petq *PartEntryTranslationQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, petq.ctx, ent.OpQueryCount)
	if err := petq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, petq, querierCount[*PartEntryTranslationQuery](), petq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (petq *PartEntryTranslationQuery) CountX(ctx context.Context) int {
	count, err := petq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (petq *PartEntryTranslationQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, petq.ctx, ent.OpQueryExist)
	switch _, err := petq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (petq *PartEntryTranslationQuery) ExistX(ctx context.Context) bool {
	exist, err := petq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PartEntryTranslationQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (petq *PartEntryTranslationQuery) Clone() *PartEntryTranslationQuery {
	if petq == nil {
		return nil
	}
	return &PartEntryTranslationQuery{
		config:        petq.config,
		ctx:           petq.ctx.Clone(),
		order:         append([]partentrytranslation.OrderOption{}, petq.order...),
		inters:        append([]Interceptor{}, petq.inters...),
		predicates:    append([]predicate.PartEntryTranslation{}, petq.predicates...),
		withPartEntry: petq.withPartEntry.Clone(),
		// clone intermediate query.
		sql:  petq.sql.Clone(),
		path: petq.path,
	}
}

// WithPartEntry tells the query-builder to eager-load the nodes that are connected to
// the "part_entry" edge. The optional arguments are used to configure the query builder of the edge.
func (petq *PartEntryTranslationQuery) WithPartEntry(opts ...func(*PartEntryQuery)) *PartEntryTranslationQuery {
	query := (&PartEntryClient{config: petq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	petq.withPartEntry = query
	return petq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		PartEntryID string `json:"part_entry_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PartEntryTranslation.Query().
//		GroupBy(partentrytranslation.FieldPartEntryID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (petq *PartEntryTranslationQuery) GroupBy(field string, fields ...string) *PartEntryTranslationGroupBy {
	petq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PartEntryTranslationGroupBy{build: petq}
	grbuild.flds = &petq.ctx.Fields
	grbuild.label = partentrytranslation.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		PartEntryID string `json:"part_entry_id,omitempty"`
//	}
//
//	client.PartEntryTranslation.Query().
//		Select(partentrytranslation.FieldPartEntryID).
//		Scan(ctx, &v)
func (petq *PartEntryTranslationQuery) Select(fields ...string) *PartEntryTranslationSelect {
	petq.ctx.Fields = append(petq.ctx.Fields, fields...)
	sbuild := &PartEntryTranslationSelect{PartEntryTranslationQuery: petq}
	sbuild.label = partentrytranslation.Label
	sbuild.flds, sbuild.scan = &petq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PartEntryTranslationSelect configured with the given aggregations.
func (petq *PartEntryTranslationQuery) Aggregate(fns ...AggregateFunc) *PartEntryTranslationSelect {
	return petq.Select().Aggregate(fns...)
}

func (petq *PartEntryTranslationQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range petq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, petq); err != nil {
				return err
			}
		}
	}
	for _, f := range petq.ctx.Fields {
		if !partentrytranslation.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if petq.path != nil {
		prev, err := petq.path(ctx)
		if err != nil {
			return err
		}
		petq.sql = prev
	}
	return nil
}

func (petq *PartEntryTranslationQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PartEntryTranslation, error) {
	var (
		nodes       = []*PartEntryTranslation{}
		_spec       = petq.querySpec()
		loadedTypes = [1]bool{
			petq.withPartEntry != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PartEntryTranslation).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PartEntryTranslation{config: petq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, petq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := petq.withPartEntry; query != nil {
		if err := petq.loadPartEntry(ctx, query, nodes, nil,
			func(n *PartEntryTranslation, e *PartEntry) { n.Edges.PartEntry = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (petq *PartEntryTranslationQuery) loadPartEntry(ctx context.Context, query *PartEntryQuery, nodes []*PartEntryTranslation, init func(*PartEntryTranslation), assign func(*PartEntryTranslation, *PartEntry)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*PartEntryTranslation)
	for i := range nodes {
		fk := nodes[i].PartEntryID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(partentry.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "part_entry_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (petq *PartEntryTranslationQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := petq.querySpec()
	_spec.Node.Columns = petq.ctx.Fields
	if len(petq.ctx.Fields) > 0 {
		_spec.Unique = petq.ctx.Unique != nil && *petq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, petq.driver, _spec)
}

func (petq *PartEntryTranslationQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(partentrytranslation.Table, partentrytranslation.Columns, sqlgraph.NewFieldSpec(partentrytranslation.FieldID, field.TypeString))
	_spec.From = petq.sql
	if unique := petq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if petq.path != nil {
		_spec.Unique = true
	}
	if fields := petq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, partentrytranslation.FieldID)
		for i := range fields {
			if fields[i] != partentrytranslation.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if petq.withPartEntry != nil {
			_spec.Node.AddColumnOnce(partentrytranslation.FieldPartEntryID)
		}
	}
	if ps := petq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := petq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := petq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := petq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (petq *PartEntryTranslationQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(petq.driver.Dialect())
	t1 := builder.Table(partentrytranslation.Table)
	columns := petq.ctx.Fields
	if len(columns) == 0 {
		columns = partentrytranslation.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if petq.sql != nil {
		selector = petq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if petq.ctx.Unique != nil && *petq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range petq.predicates {
		p(selector)
	}
	for _, p := range petq.order {
		p(selector)
	}
	if offset := petq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := petq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PartEntryTranslationGroupBy is the group-by builder for PartEntryTranslation entities.
type PartEntryTranslationGroupBy struct {
	selector
	build *PartEntryTranslationQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (petgb *PartEntryTranslationGroupBy) Aggregate(fns ...AggregateFunc) *PartEntryTranslationGroupBy {
	petgb.fns = append(petgb.fns, fns...)
	return petgb
}

// Scan applies the selector query and scans the result into the given value.
func (petgb *PartEntryTranslationGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, petgb.build.ctx, ent.OpQueryGroupBy)
	if err := petgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PartEntryTranslationQuery, *PartEntryTranslationGroupBy](ctx, petgb.build, petgb, petgb.build.inters, v)
}

func (petgb *PartEntryTranslationGroupBy) sqlScan(ctx context.Context, root *PartEntryTranslationQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(petgb.fns))
	for _, fn := range petgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*petgb.flds)+len(petgb.fns))
		for _, f := range *petgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*petgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := petgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PartEntryTranslationSelect is the builder for selecting fields of PartEntryTranslation entities.
type PartEntryTranslationSelect struct {
	*PartEntryTranslationQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pets *PartEntryTranslationSelect) Aggregate(fns ...AggregateFunc) *PartEntryTranslationSelect {
	pets.fns = append(pets.fns, fns...)
	return pets
}

// Scan applies the selector query and scans the result into the given value.
func (pets *PartEntryTranslationSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pets.ctx, ent.OpQuerySelect)
	if err := pets.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PartEntryTranslationQuery, *PartEntryTranslationSelect](ctx, pets.PartEntryTranslationQuery, pets, pets.inters, v)
}

func (pets *PartEntryTranslationSelect) sqlScan(ctx context.Context, root *PartEntryTranslationQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pets.fns))
	for _, fn := range pets.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pets.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pets.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
