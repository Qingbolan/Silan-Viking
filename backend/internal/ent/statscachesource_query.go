// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"silan-backend/internal/ent/predicate"
	"silan-backend/internal/ent/statscachesource"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// StatsCacheSourceQuery is the builder for querying StatsCacheSource entities.
type StatsCacheSourceQuery struct {
	config
	ctx        *QueryContext
	order      []statscachesource.OrderOption
	inters     []Interceptor
	predicates []predicate.StatsCacheSource
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the StatsCacheSourceQuery builder.
func (scsq *StatsCacheSourceQuery) Where(ps ...predicate.StatsCacheSource) *StatsCacheSourceQuery {
	scsq.predicates = append(scsq.predicates, ps...)
	return scsq
}

// Limit the number of records to be returned by this query.
func (scsq *StatsCacheSourceQuery) Limit(limit int) *StatsCacheSourceQuery {
	scsq.ctx.Limit = &limit
	return scsq
}

// Offset to start from.
func (scsq *StatsCacheSourceQuery) Offset(offset int) *StatsCacheSourceQuery {
	scsq.ctx.Offset = &offset
	return scsq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (scsq *StatsCacheSourceQuery) Unique(unique bool) *StatsCacheSourceQuery {
	scsq.ctx.Unique = &unique
	return scsq
}

// Order specifies how the records should be ordered.
func (scsq *StatsCacheSourceQuery) Order(o ...statscachesource.OrderOption) *StatsCacheSourceQuery {
	scsq.order = append(scsq.order, o...)
	return scsq
}

// First returns the first StatsCacheSource entity from the query.
// Returns a *NotFoundError when no StatsCacheSource was found.
func (scsq *StatsCacheSourceQuery) First(ctx context.Context) (*StatsCacheSource, error) {
	nodes, err := scsq.Limit(1).All(setContextOp(ctx, scsq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{statscachesource.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (scsq *StatsCacheSourceQuery) FirstX(ctx context.Context) *StatsCacheSource {
	node, err := scsq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first StatsCacheSource ID from the query.
// Returns a *NotFoundError when no StatsCacheSource ID was found.
func (scsq *StatsCacheSourceQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = scsq.Limit(1).IDs(setContextOp(ctx, scsq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{statscachesource.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (scsq *StatsCacheSourceQuery) FirstIDX(ctx context.Context) int {
	id, err := scsq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single StatsCacheSource entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one StatsCacheSource entity is found.
// Returns a *NotFoundError when no StatsCacheSource entities are found.
func (scsq *StatsCacheSourceQuery) Only(ctx context.Context) (*StatsCacheSource, error) {
	nodes, err := scsq.Limit(2).All(setContextOp(ctx, scsq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{statscachesource.Label}
	default:
		return nil, &NotSingularError{statscachesource.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (scsq *StatsCacheSourceQuery) OnlyX(ctx context.Context) *StatsCacheSource {
	node, err := scsq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only StatsCacheSource ID in the query.
// Returns a *NotSingularError when more than one StatsCacheSource ID is found.
// Returns a *NotFoundError when no entities are found.
func (scsq *StatsCacheSourceQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = scsq.Limit(2).IDs(setContextOp(ctx, scsq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{statscachesource.Label}
	default:
		err = &NotSingularError{statscachesource.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (scsq *StatsCacheSourceQuery) OnlyIDX(ctx context.Context) int {
	id, err := scsq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of StatsCacheSources.
func (scsq *StatsCacheSourceQuery) All(ctx context.Context) ([]*StatsCacheSource, error) {
	ctx = setContextOp(ctx, scsq.ctx, ent.OpQueryAll)
	if err := scsq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*StatsCacheSource, *StatsCacheSourceQuery]()
	return withInterceptors[[]*StatsCacheSource](ctx, scsq, qr, scsq.inters)
}

// AllX is like All, but panics if an error occurs.
func (scsq *StatsCacheSourceQuery) AllX(ctx context.Context) []*StatsCacheSource {
	nodes, err := scsq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of StatsCacheSource IDs.
func (scsq *StatsCacheSourceQuery) IDs(ctx context.Context) (ids []int, err error) {
	if scsq.ctx.Unique == nil && scsq.path != nil {
		scsq.Unique(true)
	}
	ctx = setContextOp(ctx, scsq.ctx, ent.OpQueryIDs)
	if err = scsq.Select(statscachesource.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (scsq *StatsCacheSourceQuery) IDsX(ctx context.Context) []int {
	ids, err := scsq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (scsq *StatsCacheSourceQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, scsq.ctx, ent.OpQueryCount)
	if err := scsq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, scsq, querierCount[*StatsCacheSourceQuery](), scsq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (scsq *StatsCacheSourceQuery) CountX(ctx context.Context) int {
	count, err := scsq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (scsq *StatsCacheSourceQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, scsq.ctx, ent.OpQueryExist)
	switch _, err := scsq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (scsq *StatsCacheSourceQuery) ExistX(ctx context.Context) bool {
	exist, err := scsq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the StatsCacheSourceQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (scsq *StatsCacheSourceQuery) Clone() *StatsCacheSourceQuery {
	if scsq == nil {
		return nil
	}
	return &StatsCacheSourceQuery{
		config:     scsq.config,
		ctx:        scsq.ctx.Clone(),
		order:      append([]statscachesource.OrderOption{}, scsq.order...),
		inters:     append([]Interceptor{}, scsq.inters...),
		predicates: append([]predicate.StatsCacheSource{}, scsq.predicates...),
		// clone intermediate query.
		sql:  scsq.sql.Clone(),
		path: scsq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		EntityType statscachesource.EntityType `json:"entity_type,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.StatsCacheSource.Query().
//		GroupBy(statscachesource.FieldEntityType).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (scsq *StatsCacheSourceQuery) GroupBy(field string, fields ...string) *StatsCacheSourceGroupBy {
	scsq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &StatsCacheSourceGroupBy{build: scsq}
	grbuild.flds = &scsq.ctx.Fields
	grbuild.label = statscachesource.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		EntityType statscachesource.EntityType `json:"entity_type,omitempty"`
//	}
//
//	client.StatsCacheSource.Query().
//		Select(statscachesource.FieldEntityType).
//		Scan(ctx, &v)
func (scsq *StatsCacheSourceQuery) Select(fields ...string) *StatsCacheSourceSelect {
	scsq.ctx.Fields = append(scsq.ctx.Fields, fields...)
	sbuild := &StatsCacheSourceSelect{StatsCacheSourceQuery: scsq}
	sbuild.label = statscachesource.Label
	sbuild.flds, sbuild.scan = &scsq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a StatsCacheSourceSelect configured with the given aggregations.
func (scsq *StatsCacheSourceQuery) Aggregate(fns ...AggregateFunc) *StatsCacheSourceSelect {
	return scsq.Select().Aggregate(fns...)
}

func (scsq *StatsCacheSourceQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range scsq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, scsq); err != nil {
				return err
			}
		}
	}
	for _, f := range scsq.ctx.Fields {
		if !statscachesource.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if scsq.path != nil {
		prev, err := scsq.path(ctx)
		if err != nil {
			return err
		}
		scsq.sql = prev
	}
	return nil
}

func (scsq *StatsCacheSourceQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*StatsCacheSource, error) {
	var (
		nodes = []*StatsCacheSource{}
		_spec = scsq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*StatsCacheSource).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &StatsCacheSource{config: scsq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, scsq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (scsq *StatsCacheSourceQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := scsq.querySpec()
	_spec.Node.Columns = scsq.ctx.Fields
	if len(scsq.ctx.Fields) > 0 {
		_spec.Unique = scsq.ctx.Unique != nil && *scsq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, scsq.driver, _spec)
}

func (scsq *StatsCacheSourceQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(statscachesource.Table, statscachesource.Columns, sqlgraph.NewFieldSpec(statscachesource.FieldID, field.TypeInt))
	_spec.From = scsq.sql
	if unique := scsq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if scsq.path != nil {
		_spec.Unique = true
	}
	if fields := scsq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, statscachesource.FieldID)
		for i := range fields {
			if fields[i] != statscachesource.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := scsq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := scsq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := scsq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := scsq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (scsq *StatsCacheSourceQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(scsq.driver.Dialect())
	t1 := builder.Table(statscachesource.Table)
	columns := scsq.ctx.Fields
	if len(columns) == 0 {
		columns = statscachesource.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if scsq.sql != nil {
		selector = scsq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if scsq.ctx.Unique != nil && *scsq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range scsq.predicates {
		p(selector)
	}
	for _, p := range scsq.order {
		p(selector)
	}
	if offset := scsq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := scsq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// StatsCacheSourceGroupBy is the group-by builder for StatsCacheSource entities.
type StatsCacheSourceGroupBy struct {
	selector
	build *StatsCacheSourceQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (scsgb *StatsCacheSourceGroupBy) Aggregate(fns ...AggregateFunc) *StatsCacheSourceGroupBy {
	scsgb.fns = append(scsgb.fns, fns...)
	return scsgb
}

// Scan applies the selector query and scans the result into the given value.
func (scsgb *StatsCacheSourceGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, scsgb.build.ctx, ent.OpQueryGroupBy)
	if err := scsgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StatsCacheSourceQuery, *StatsCacheSourceGroupBy](ctx, scsgb.build, scsgb, scsgb.build.inters, v)
}

func (scsgb *StatsCacheSourceGroupBy) sqlScan(ctx context.Context, root *StatsCacheSourceQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(scsgb.fns))
	for _, fn := range scsgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*scsgb.flds)+len(scsgb.fns))
		for _, f := range *scsgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*scsgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := scsgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// StatsCacheSourceSelect is the builder for selecting fields of StatsCacheSource entities.
type StatsCacheSourceSelect struct {
	*StatsCacheSourceQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (scss *StatsCacheSourceSelect) Aggregate(fns ...AggregateFunc) *StatsCacheSourceSelect {
	scss.fns = append(scss.fns, fns...)
	return scss
}

// Scan applies the selector query and scans the result into the given value.
func (scss *StatsCacheSourceSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, scss.ctx, ent.OpQuerySelect)
	if err := scss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StatsCacheSourceQuery, *StatsCacheSourceSelect](ctx, scss.StatsCacheSourceQuery, scss, scss.inters, v)
}

func (scss *StatsCacheSourceSelect) sqlScan(ctx context.Context, root *StatsCacheSourceQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(scss.fns))
	for _, fn := range scss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*scss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := scss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
