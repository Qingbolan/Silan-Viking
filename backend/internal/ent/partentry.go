// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"silan-backend/internal/ent/itempart"
	"silan-backend/internal/ent/partentry"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// PartEntry is the model entity for the PartEntry schema.
type PartEntry struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// ItemPartID holds the value of the "item_part_id" field.
	ItemPartID uuid.UUID `json:"item_part_id,omitempty"`
	// EntryID holds the value of the "entry_id" field.
	EntryID string `json:"entry_id,omitempty"`
	// SortOrder holds the value of the "sort_order" field.
	SortOrder int `json:"sort_order,omitempty"`
	// SharedPayload holds the value of the "shared_payload" field.
	SharedPayload map[string]interface{} `json:"shared_payload,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the PartEntryQuery when eager-loading is set.
	Edges        PartEntryEdges `json:"edges"`
	selectValues sql.SelectValues
}

// PartEntryEdges holds the relations/edges for other nodes in the graph.
type PartEntryEdges struct {
	// ItemPart holds the value of the item_part edge.
	ItemPart *ItemPart `json:"item_part,omitempty"`
	// Translations holds the value of the translations edge.
	Translations []*PartEntryTranslation `json:"translations,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// ItemPartOrErr returns the ItemPart value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e PartEntryEdges) ItemPartOrErr() (*ItemPart, error) {
	if e.ItemPart != nil {
		return e.ItemPart, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: itempart.Label}
	}
	return nil, &NotLoadedError{edge: "item_part"}
}

// TranslationsOrErr returns the Translations value or an error if the edge
// was not loaded in eager-loading.
func (e PartEntryEdges) TranslationsOrErr() ([]*PartEntryTranslation, error) {
	if e.loadedTypes[1] {
		return e.Translations, nil
	}
	return nil, &NotLoadedError{edge: "translations"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*PartEntry) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case partentry.FieldSharedPayload:
			values[i] = new([]byte)
		case partentry.FieldSortOrder:
			values[i] = new(sql.NullInt64)
		case partentry.FieldEntryID:
			values[i] = new(sql.NullString)
		case partentry.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case partentry.FieldID, partentry.FieldItemPartID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the PartEntry fields.
func (pe *PartEntry) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case partentry.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				pe.ID = *value
			}
		case partentry.FieldItemPartID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field item_part_id", values[i])
			} else if value != nil {
				pe.ItemPartID = *value
			}
		case partentry.FieldEntryID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field entry_id", values[i])
			} else if value.Valid {
				pe.EntryID = value.String
			}
		case partentry.FieldSortOrder:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field sort_order", values[i])
			} else if value.Valid {
				pe.SortOrder = int(value.Int64)
			}
		case partentry.FieldSharedPayload:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field shared_payload", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &pe.SharedPayload); err != nil {
					return fmt.Errorf("unmarshal field shared_payload: %w", err)
				}
			}
		case partentry.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				pe.CreatedAt = value.Time
			}
		default:
			pe.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the PartEntry.
// This includes values selected through modifiers, order, etc.
func (pe *PartEntry) Value(name string) (ent.Value, error) {
	return pe.selectValues.Get(name)
}

// QueryItemPart queries the "item_part" edge of the PartEntry entity.
func (pe *PartEntry) QueryItemPart() *ItemPartQuery {
	return NewPartEntryClient(pe.config).QueryItemPart(pe)
}

// QueryTranslations queries the "translations" edge of the PartEntry entity.
func (pe *PartEntry) QueryTranslations() *PartEntryTranslationQuery {
	return NewPartEntryClient(pe.config).QueryTranslations(pe)
}

// Update returns a builder for updating this PartEntry.
// Note that you need to call PartEntry.Unwrap() before calling this method if this PartEntry
// was returned from a transaction, and the transaction was committed or rolled back.
func (pe *PartEntry) Update() *PartEntryUpdateOne {
	return NewPartEntryClient(pe.config).UpdateOne(pe)
}

// Unwrap unwraps the PartEntry entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pe *PartEntry) Unwrap() *PartEntry {
	_tx, ok := pe.config.driver.(*txDriver)
	if !ok {
		panic("ent: PartEntry is not a transactional entity")
	}
	pe.config.driver = _tx.drv
	return pe
}

// String implements the fmt.Stringer.
func (pe *PartEntry) String() string {
	var builder strings.Builder
	builder.WriteString("PartEntry(")
	builder.WriteString(fmt.Sprintf("id=%v, ", pe.ID))
	builder.WriteString("item_part_id=")
	builder.WriteString(fmt.Sprintf("%v", pe.ItemPartID))
	builder.WriteString(", ")
	builder.WriteString("entry_id=")
	builder.WriteString(pe.EntryID)
	builder.WriteString(", ")
	builder.WriteString("sort_order=")
	builder.WriteString(fmt.Sprintf("%v", pe.SortOrder))
	builder.WriteString(", ")
	builder.WriteString("shared_payload=")
	builder.WriteString(fmt.Sprintf("%v", pe.SharedPayload))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(pe.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// PartEntries is a parsable slice of PartEntry.
type PartEntries []*PartEntry
