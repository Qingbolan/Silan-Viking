// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"silan-backend/internal/ent/itempart"
	"silan-backend/internal/ent/partentry"
	"silan-backend/internal/ent/partentrytranslation"
	"time"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PartEntryCreate is the builder for creating a PartEntry entity.
type PartEntryCreate struct {
	config
	mutation *PartEntryMutation
	hooks    []Hook
}

// SetItemPartID sets the "item_part_id" field.
func (pec *PartEntryCreate) SetItemPartID(s string) *PartEntryCreate {
	pec.mutation.SetItemPartID(s)
	return pec
}

// SetEntryID sets the "entry_id" field.
func (pec *PartEntryCreate) SetEntryID(s string) *PartEntryCreate {
	pec.mutation.SetEntryID(s)
	return pec
}

// SetSortOrder sets the "sort_order" field.
func (pec *PartEntryCreate) SetSortOrder(i int) *PartEntryCreate {
	pec.mutation.SetSortOrder(i)
	return pec
}

// SetNillableSortOrder sets the "sort_order" field if the given value is not nil.
func (pec *PartEntryCreate) SetNillableSortOrder(i *int) *PartEntryCreate {
	if i != nil {
		pec.SetSortOrder(*i)
	}
	return pec
}

// SetSharedPayload sets the "shared_payload" field.
func (pec *PartEntryCreate) SetSharedPayload(m map[string]interface{}) *PartEntryCreate {
	pec.mutation.SetSharedPayload(m)
	return pec
}

// SetCreatedAt sets the "created_at" field.
func (pec *PartEntryCreate) SetCreatedAt(t time.Time) *PartEntryCreate {
	pec.mutation.SetCreatedAt(t)
	return pec
}

// SetNillableCreatedAt sets the "created_at" field if the given value is not nil.
func (pec *PartEntryCreate) SetNillableCreatedAt(t *time.Time) *PartEntryCreate {
	if t != nil {
		pec.SetCreatedAt(*t)
	}
	return pec
}

// SetID sets the "id" field.
func (pec *PartEntryCreate) SetID(s string) *PartEntryCreate {
	pec.mutation.SetID(s)
	return pec
}

// SetNillableID sets the "id" field if the given value is not nil.
func (pec *PartEntryCreate) SetNillableID(s *string) *PartEntryCreate {
	if s != nil {
		pec.SetID(*s)
	}
	return pec
}

// SetItemPart sets the "item_part" edge to the ItemPart entity.
func (pec *PartEntryCreate) SetItemPart(i *ItemPart) *PartEntryCreate {
	return pec.SetItemPartID(i.ID)
}

// AddTranslationIDs adds the "translations" edge to the PartEntryTranslation entity by IDs.
func (pec *PartEntryCreate) AddTranslationIDs(ids ...string) *PartEntryCreate {
	pec.mutation.AddTranslationIDs(ids...)
	return pec
}

// AddTranslations adds the "translations" edges to the PartEntryTranslation entity.
func (pec *PartEntryCreate) AddTranslations(p ...*PartEntryTranslation) *PartEntryCreate {
	ids := make([]string, len(p))
	for i := range p {
		ids[i] = p[i].ID
	}
	return pec.AddTranslationIDs(ids...)
}

// Mutation returns the PartEntryMutation object of the builder.
func (pec *PartEntryCreate) Mutation() *PartEntryMutation {
	return pec.mutation
}

// Save creates the PartEntry in the database.
func (pec *PartEntryCreate) Save(ctx context.Context) (*PartEntry, error) {
	pec.defaults()
	return withHooks(ctx, pec.sqlSave, pec.mutation, pec.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (pec *PartEntryCreate) SaveX(ctx context.Context) *PartEntry {
	v, err := pec.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pec *PartEntryCreate) Exec(ctx context.Context) error {
	_, err := pec.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pec *PartEntryCreate) ExecX(ctx context.Context) {
	if err := pec.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (pec *PartEntryCreate) defaults() {
	if _, ok := pec.mutation.SortOrder(); !ok {
		v := partentry.DefaultSortOrder
		pec.mutation.SetSortOrder(v)
	}
	if _, ok := pec.mutation.CreatedAt(); !ok {
		v := partentry.DefaultCreatedAt()
		pec.mutation.SetCreatedAt(v)
	}
	if _, ok := pec.mutation.ID(); !ok {
		v := partentry.DefaultID()
		pec.mutation.SetID(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (pec *PartEntryCreate) check() error {
	if _, ok := pec.mutation.ItemPartID(); !ok {
		return &ValidationError{Name: "item_part_id", err: errors.New(`ent: missing required field "PartEntry.item_part_id"`)}
	}
	if _, ok := pec.mutation.EntryID(); !ok {
		return &ValidationError{Name: "entry_id", err: errors.New(`ent: missing required field "PartEntry.entry_id"`)}
	}
	if _, ok := pec.mutation.SortOrder(); !ok {
		return &ValidationError{Name: "sort_order", err: errors.New(`ent: missing required field "PartEntry.sort_order"`)}
	}
	if _, ok := pec.mutation.SharedPayload(); !ok {
		return &ValidationError{Name: "shared_payload", err: errors.New(`ent: missing required field "PartEntry.shared_payload"`)}
	}
	if len(pec.mutation.ItemPartIDs()) == 0 {
		return &ValidationError{Name: "item_part", err: errors.New(`ent: missing required edge "PartEntry.item_part"`)}
	}
	return nil
}

func (pec *PartEntryCreate) sqlSave(ctx context.Context) (*PartEntry, error) {
	if err := pec.check(); err != nil {
		return nil, err
	}
	_node, _spec := pec.createSpec()
	if err := sqlgraph.CreateNode(ctx, pec.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != nil {
		if id, ok := _spec.ID.Value.(string); ok {
			_node.ID = id
		} else {
			return nil, fmt.Errorf("unexpected PartEntry.ID type: %T", _spec.ID.Value)
		}
	}
	pec.mutation.id = &_node.ID
	pec.mutation.done = true
	return _node, nil
}

func (pec *PartEntryCreate) createSpec() (*PartEntry, *sqlgraph.CreateSpec) {
	var (
		_node = &PartEntry{config: pec.config}
		_spec = sqlgraph.NewCreateSpec(partentry.Table, sqlgraph.NewFieldSpec(partentry.FieldID, field.TypeString))
	)
	if id, ok := pec.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = id
	}
	if value, ok := pec.mutation.EntryID(); ok {
		_spec.SetField(partentry.FieldEntryID, field.TypeString, value)
		_node.EntryID = value
	}
	if value, ok := pec.mutation.SortOrder(); ok {
		_spec.SetField(partentry.FieldSortOrder, field.TypeInt, value)
		_node.SortOrder = value
	}
	if value, ok := pec.mutation.SharedPayload(); ok {
		_spec.SetField(partentry.FieldSharedPayload, field.TypeJSON, value)
		_node.SharedPayload = value
	}
	if value, ok := pec.mutation.CreatedAt(); ok {
		_spec.SetField(partentry.FieldCreatedAt, field.TypeTime, value)
		_node.CreatedAt = value
	}
	if nodes := pec.mutation.ItemPartIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   partentry.ItemPartTable,
			Columns: []string{partentry.ItemPartColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(itempart.FieldID, field.TypeString),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.ItemPartID = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := pec.mutation.TranslationsIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   partentry.TranslationsTable,
			Columns: []string{partentry.TranslationsColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(partentrytranslation.FieldID, field.TypeString),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// PartEntryCreateBulk is the builder for creating many PartEntry entities in bulk.
type PartEntryCreateBulk struct {
	config
	err      error
	builders []*PartEntryCreate
}

// Save creates the PartEntry entities in the database.
func (pecb *PartEntryCreateBulk) Save(ctx context.Context) ([]*PartEntry, error) {
	if pecb.err != nil {
		return nil, pecb.err
	}
	specs := make([]*sqlgraph.CreateSpec, len(pecb.builders))
	nodes := make([]*PartEntry, len(pecb.builders))
	mutators := make([]Mutator, len(pecb.builders))
	for i := range pecb.builders {
		func(i int, root context.Context) {
			builder := pecb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*PartEntryMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, pecb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, pecb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, pecb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (pecb *PartEntryCreateBulk) SaveX(ctx context.Context) []*PartEntry {
	v, err := pecb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pecb *PartEntryCreateBulk) Exec(ctx context.Context) error {
	_, err := pecb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pecb *PartEntryCreateBulk) ExecX(ctx context.Context) {
	if err := pecb.Exec(ctx); err != nil {
		panic(err)
	}
}
