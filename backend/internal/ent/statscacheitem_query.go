// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"silan-backend/internal/ent/predicate"
	"silan-backend/internal/ent/statscacheitem"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// StatsCacheItemQuery is the builder for querying StatsCacheItem entities.
type StatsCacheItemQuery struct {
	config
	ctx        *QueryContext
	order      []statscacheitem.OrderOption
	inters     []Interceptor
	predicates []predicate.StatsCacheItem
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the StatsCacheItemQuery builder.
func (sciq *StatsCacheItemQuery) Where(ps ...predicate.StatsCacheItem) *StatsCacheItemQuery {
	sciq.predicates = append(sciq.predicates, ps...)
	return sciq
}

// Limit the number of records to be returned by this query.
func (sciq *StatsCacheItemQuery) Limit(limit int) *StatsCacheItemQuery {
	sciq.ctx.Limit = &limit
	return sciq
}

// Offset to start from.
func (sciq *StatsCacheItemQuery) Offset(offset int) *StatsCacheItemQuery {
	sciq.ctx.Offset = &offset
	return sciq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (sciq *StatsCacheItemQuery) Unique(unique bool) *StatsCacheItemQuery {
	sciq.ctx.Unique = &unique
	return sciq
}

// Order specifies how the records should be ordered.
func (sciq *StatsCacheItemQuery) Order(o ...statscacheitem.OrderOption) *StatsCacheItemQuery {
	sciq.order = append(sciq.order, o...)
	return sciq
}

// First returns the first StatsCacheItem entity from the query.
// Returns a *NotFoundError when no StatsCacheItem was found.
func (sciq *StatsCacheItemQuery) First(ctx context.Context) (*StatsCacheItem, error) {
	nodes, err := sciq.Limit(1).All(setContextOp(ctx, sciq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{statscacheitem.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (sciq *StatsCacheItemQuery) FirstX(ctx context.Context) *StatsCacheItem {
	node, err := sciq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first StatsCacheItem ID from the query.
// Returns a *NotFoundError when no StatsCacheItem ID was found.
func (sciq *StatsCacheItemQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = sciq.Limit(1).IDs(setContextOp(ctx, sciq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{statscacheitem.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (sciq *StatsCacheItemQuery) FirstIDX(ctx context.Context) int {
	id, err := sciq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single StatsCacheItem entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one StatsCacheItem entity is found.
// Returns a *NotFoundError when no StatsCacheItem entities are found.
func (sciq *StatsCacheItemQuery) Only(ctx context.Context) (*StatsCacheItem, error) {
	nodes, err := sciq.Limit(2).All(setContextOp(ctx, sciq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{statscacheitem.Label}
	default:
		return nil, &NotSingularError{statscacheitem.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (sciq *StatsCacheItemQuery) OnlyX(ctx context.Context) *StatsCacheItem {
	node, err := sciq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only StatsCacheItem ID in the query.
// Returns a *NotSingularError when more than one StatsCacheItem ID is found.
// Returns a *NotFoundError when no entities are found.
func (sciq *StatsCacheItemQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = sciq.Limit(2).IDs(setContextOp(ctx, sciq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{statscacheitem.Label}
	default:
		err = &NotSingularError{statscacheitem.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (sciq *StatsCacheItemQuery) OnlyIDX(ctx context.Context) int {
	id, err := sciq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of StatsCacheItems.
func (sciq *StatsCacheItemQuery) All(ctx context.Context) ([]*StatsCacheItem, error) {
	ctx = setContextOp(ctx, sciq.ctx, ent.OpQueryAll)
	if err := sciq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*StatsCacheItem, *StatsCacheItemQuery]()
	return withInterceptors[[]*StatsCacheItem](ctx, sciq, qr, sciq.inters)
}

// AllX is like All, but panics if an error occurs.
func (sciq *StatsCacheItemQuery) AllX(ctx context.Context) []*StatsCacheItem {
	nodes, err := sciq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of StatsCacheItem IDs.
func (sciq *StatsCacheItemQuery) IDs(ctx context.Context) (ids []int, err error) {
	if sciq.ctx.Unique == nil && sciq.path != nil {
		sciq.Unique(true)
	}
	ctx = setContextOp(ctx, sciq.ctx, ent.OpQueryIDs)
	if err = sciq.Select(statscacheitem.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (sciq *StatsCacheItemQuery) IDsX(ctx context.Context) []int {
	ids, err := sciq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (sciq *StatsCacheItemQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, sciq.ctx, ent.OpQueryCount)
	if err := sciq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, sciq, querierCount[*StatsCacheItemQuery](), sciq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (sciq *StatsCacheItemQuery) CountX(ctx context.Context) int {
	count, err := sciq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (sciq *StatsCacheItemQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, sciq.ctx, ent.OpQueryExist)
	switch _, err := sciq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (sciq *StatsCacheItemQuery) ExistX(ctx context.Context) bool {
	exist, err := sciq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the StatsCacheItemQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (sciq *StatsCacheItemQuery) Clone() *StatsCacheItemQuery {
	if sciq == nil {
		return nil
	}
	return &StatsCacheItemQuery{
		config:     sciq.config,
		ctx:        sciq.ctx.Clone(),
		order:      append([]statscacheitem.OrderOption{}, sciq.order...),
		inters:     append([]Interceptor{}, sciq.inters...),
		predicates: append([]predicate.StatsCacheItem{}, sciq.predicates...),
		// clone intermediate query.
		sql:  sciq.sql.Clone(),
		path: sciq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		EntityType statscacheitem.EntityType `json:"entity_type,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.StatsCacheItem.Query().
//		GroupBy(statscacheitem.FieldEntityType).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (sciq *StatsCacheItemQuery) GroupBy(field string, fields ...string) *StatsCacheItemGroupBy {
	sciq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &StatsCacheItemGroupBy{build: sciq}
	grbuild.flds = &sciq.ctx.Fields
	grbuild.label = statscacheitem.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		EntityType statscacheitem.EntityType `json:"entity_type,omitempty"`
//	}
//
//	client.StatsCacheItem.Query().
//		Select(statscacheitem.FieldEntityType).
//		Scan(ctx, &v)
func (sciq *StatsCacheItemQuery) Select(fields ...string) *StatsCacheItemSelect {
	sciq.ctx.Fields = append(sciq.ctx.Fields, fields...)
	sbuild := &StatsCacheItemSelect{StatsCacheItemQuery: sciq}
	sbuild.label = statscacheitem.Label
	sbuild.flds, sbuild.scan = &sciq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a StatsCacheItemSelect configured with the given aggregations.
func (sciq *StatsCacheItemQuery) Aggregate(fns ...AggregateFunc) *StatsCacheItemSelect {
	return sciq.Select().Aggregate(fns...)
}

func (sciq *StatsCacheItemQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range sciq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, sciq); err != nil {
				return err
			}
		}
	}
	for _, f := range sciq.ctx.Fields {
		if !statscacheitem.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if sciq.path != nil {
		prev, err := sciq.path(ctx)
		if err != nil {
			return err
		}
		sciq.sql = prev
	}
	return nil
}

func (sciq *StatsCacheItemQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*StatsCacheItem, error) {
	var (
		nodes = []*StatsCacheItem{}
		_spec = sciq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*StatsCacheItem).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &StatsCacheItem{config: sciq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, sciq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (sciq *StatsCacheItemQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := sciq.querySpec()
	_spec.Node.Columns = sciq.ctx.Fields
	if len(sciq.ctx.Fields) > 0 {
		_spec.Unique = sciq.ctx.Unique != nil && *sciq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, sciq.driver, _spec)
}

func (sciq *StatsCacheItemQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(statscacheitem.Table, statscacheitem.Columns, sqlgraph.NewFieldSpec(statscacheitem.FieldID, field.TypeInt))
	_spec.From = sciq.sql
	if unique := sciq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if sciq.path != nil {
		_spec.Unique = true
	}
	if fields := sciq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, statscacheitem.FieldID)
		for i := range fields {
			if fields[i] != statscacheitem.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := sciq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := sciq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := sciq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := sciq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (sciq *StatsCacheItemQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(sciq.driver.Dialect())
	t1 := builder.Table(statscacheitem.Table)
	columns := sciq.ctx.Fields
	if len(columns) == 0 {
		columns = statscacheitem.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if sciq.sql != nil {
		selector = sciq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if sciq.ctx.Unique != nil && *sciq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range sciq.predicates {
		p(selector)
	}
	for _, p := range sciq.order {
		p(selector)
	}
	if offset := sciq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := sciq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// StatsCacheItemGroupBy is the group-by builder for StatsCacheItem entities.
type StatsCacheItemGroupBy struct {
	selector
	build *StatsCacheItemQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (scigb *StatsCacheItemGroupBy) Aggregate(fns ...AggregateFunc) *StatsCacheItemGroupBy {
	scigb.fns = append(scigb.fns, fns...)
	return scigb
}

// Scan applies the selector query and scans the result into the given value.
func (scigb *StatsCacheItemGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, scigb.build.ctx, ent.OpQueryGroupBy)
	if err := scigb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StatsCacheItemQuery, *StatsCacheItemGroupBy](ctx, scigb.build, scigb, scigb.build.inters, v)
}

func (scigb *StatsCacheItemGroupBy) sqlScan(ctx context.Context, root *StatsCacheItemQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(scigb.fns))
	for _, fn := range scigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*scigb.flds)+len(scigb.fns))
		for _, f := range *scigb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*scigb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := scigb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// StatsCacheItemSelect is the builder for selecting fields of StatsCacheItem entities.
type StatsCacheItemSelect struct {
	*StatsCacheItemQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (scis *StatsCacheItemSelect) Aggregate(fns ...AggregateFunc) *StatsCacheItemSelect {
	scis.fns = append(scis.fns, fns...)
	return scis
}

// Scan applies the selector query and scans the result into the given value.
func (scis *StatsCacheItemSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, scis.ctx, ent.OpQuerySelect)
	if err := scis.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StatsCacheItemQuery, *StatsCacheItemSelect](ctx, scis.StatsCacheItemQuery, scis, scis.inters, v)
}

func (scis *StatsCacheItemSelect) sqlScan(ctx context.Context, root *StatsCacheItemQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(scis.fns))
	for _, fn := range scis.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*scis.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := scis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
