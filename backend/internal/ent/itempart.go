// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"silan-backend/internal/ent/itempart"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// ItemPart is the model entity for the ItemPart schema.
type ItemPart struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// PartID holds the value of the "part_id" field.
	PartID string `json:"part_id,omitempty"`
	// EntityType holds the value of the "entity_type" field.
	EntityType itempart.EntityType `json:"entity_type,omitempty"`
	// EntityID holds the value of the "entity_id" field.
	EntityID uuid.UUID `json:"entity_id,omitempty"`
	// Role holds the value of the "role" field.
	Role string `json:"role,omitempty"`
	// SortOrder holds the value of the "sort_order" field.
	SortOrder int `json:"sort_order,omitempty"`
	// CanonicalLang holds the value of the "canonical_lang" field.
	CanonicalLang string `json:"canonical_lang,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ItemPartQuery when eager-loading is set.
	Edges        ItemPartEdges `json:"edges"`
	selectValues sql.SelectValues
}

// ItemPartEdges holds the relations/edges for other nodes in the graph.
type ItemPartEdges struct {
	// Translations holds the value of the translations edge.
	Translations []*ItemPartTranslation `json:"translations,omitempty"`
	// Entries holds the value of the entries edge.
	Entries []*PartEntry `json:"entries,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// TranslationsOrErr returns the Translations value or an error if the edge
// was not loaded in eager-loading.
func (e ItemPartEdges) TranslationsOrErr() ([]*ItemPartTranslation, error) {
	if e.loadedTypes[0] {
		return e.Translations, nil
	}
	return nil, &NotLoadedError{edge: "translations"}
}

// EntriesOrErr returns the Entries value or an error if the edge
// was not loaded in eager-loading.
func (e ItemPartEdges) EntriesOrErr() ([]*PartEntry, error) {
	if e.loadedTypes[1] {
		return e.Entries, nil
	}
	return nil, &NotLoadedError{edge: "entries"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ItemPart) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case itempart.FieldSortOrder:
			values[i] = new(sql.NullInt64)
		case itempart.FieldPartID, itempart.FieldEntityType, itempart.FieldRole, itempart.FieldCanonicalLang:
			values[i] = new(sql.NullString)
		case itempart.FieldCreatedAt, itempart.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case itempart.FieldID, itempart.FieldEntityID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ItemPart fields.
func (ip *ItemPart) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case itempart.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				ip.ID = *value
			}
		case itempart.FieldPartID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field part_id", values[i])
			} else if value.Valid {
				ip.PartID = value.String
			}
		case itempart.FieldEntityType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field entity_type", values[i])
			} else if value.Valid {
				ip.EntityType = itempart.EntityType(value.String)
			}
		case itempart.FieldEntityID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field entity_id", values[i])
			} else if value != nil {
				ip.EntityID = *value
			}
		case itempart.FieldRole:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field role", values[i])
			} else if value.Valid {
				ip.Role = value.String
			}
		case itempart.FieldSortOrder:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field sort_order", values[i])
			} else if value.Valid {
				ip.SortOrder = int(value.Int64)
			}
		case itempart.FieldCanonicalLang:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field canonical_lang", values[i])
			} else if value.Valid {
				ip.CanonicalLang = value.String
			}
		case itempart.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				ip.CreatedAt = value.Time
			}
		case itempart.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				ip.UpdatedAt = value.Time
			}
		default:
			ip.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ItemPart.
// This includes values selected through modifiers, order, etc.
func (ip *ItemPart) Value(name string) (ent.Value, error) {
	return ip.selectValues.Get(name)
}

// QueryTranslations queries the "translations" edge of the ItemPart entity.
func (ip *ItemPart) QueryTranslations() *ItemPartTranslationQuery {
	return NewItemPartClient(ip.config).QueryTranslations(ip)
}

// QueryEntries queries the "entries" edge of the ItemPart entity.
func (ip *ItemPart) QueryEntries() *PartEntryQuery {
	return NewItemPartClient(ip.config).QueryEntries(ip)
}

// Update returns a builder for updating this ItemPart.
// Note that you need to call ItemPart.Unwrap() before calling this method if this ItemPart
// was returned from a transaction, and the transaction was committed or rolled back.
func (ip *ItemPart) Update() *ItemPartUpdateOne {
	return NewItemPartClient(ip.config).UpdateOne(ip)
}

// Unwrap unwraps the ItemPart entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ip *ItemPart) Unwrap() *ItemPart {
	_tx, ok := ip.config.driver.(*txDriver)
	if !ok {
		panic("ent: ItemPart is not a transactional entity")
	}
	ip.config.driver = _tx.drv
	return ip
}

// String implements the fmt.Stringer.
func (ip *ItemPart) String() string {
	var builder strings.Builder
	builder.WriteString("ItemPart(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ip.ID))
	builder.WriteString("part_id=")
	builder.WriteString(ip.PartID)
	builder.WriteString(", ")
	builder.WriteString("entity_type=")
	builder.WriteString(fmt.Sprintf("%v", ip.EntityType))
	builder.WriteString(", ")
	builder.WriteString("entity_id=")
	builder.WriteString(fmt.Sprintf("%v", ip.EntityID))
	builder.WriteString(", ")
	builder.WriteString("role=")
	builder.WriteString(ip.Role)
	builder.WriteString(", ")
	builder.WriteString("sort_order=")
	builder.WriteString(fmt.Sprintf("%v", ip.SortOrder))
	builder.WriteString(", ")
	builder.WriteString("canonical_lang=")
	builder.WriteString(ip.CanonicalLang)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(ip.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(ip.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// ItemParts is a parsable slice of ItemPart.
type ItemParts []*ItemPart
