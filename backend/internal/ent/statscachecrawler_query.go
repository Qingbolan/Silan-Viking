// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"silan-backend/internal/ent/predicate"
	"silan-backend/internal/ent/statscachecrawler"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// StatsCacheCrawlerQuery is the builder for querying StatsCacheCrawler entities.
type StatsCacheCrawlerQuery struct {
	config
	ctx        *QueryContext
	order      []statscachecrawler.OrderOption
	inters     []Interceptor
	predicates []predicate.StatsCacheCrawler
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the StatsCacheCrawlerQuery builder.
func (sccq *StatsCacheCrawlerQuery) Where(ps ...predicate.StatsCacheCrawler) *StatsCacheCrawlerQuery {
	sccq.predicates = append(sccq.predicates, ps...)
	return sccq
}

// Limit the number of records to be returned by this query.
func (sccq *StatsCacheCrawlerQuery) Limit(limit int) *StatsCacheCrawlerQuery {
	sccq.ctx.Limit = &limit
	return sccq
}

// Offset to start from.
func (sccq *StatsCacheCrawlerQuery) Offset(offset int) *StatsCacheCrawlerQuery {
	sccq.ctx.Offset = &offset
	return sccq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (sccq *StatsCacheCrawlerQuery) Unique(unique bool) *StatsCacheCrawlerQuery {
	sccq.ctx.Unique = &unique
	return sccq
}

// Order specifies how the records should be ordered.
func (sccq *StatsCacheCrawlerQuery) Order(o ...statscachecrawler.OrderOption) *StatsCacheCrawlerQuery {
	sccq.order = append(sccq.order, o...)
	return sccq
}

// First returns the first StatsCacheCrawler entity from the query.
// Returns a *NotFoundError when no StatsCacheCrawler was found.
func (sccq *StatsCacheCrawlerQuery) First(ctx context.Context) (*StatsCacheCrawler, error) {
	nodes, err := sccq.Limit(1).All(setContextOp(ctx, sccq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{statscachecrawler.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (sccq *StatsCacheCrawlerQuery) FirstX(ctx context.Context) *StatsCacheCrawler {
	node, err := sccq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first StatsCacheCrawler ID from the query.
// Returns a *NotFoundError when no StatsCacheCrawler ID was found.
func (sccq *StatsCacheCrawlerQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = sccq.Limit(1).IDs(setContextOp(ctx, sccq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{statscachecrawler.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (sccq *StatsCacheCrawlerQuery) FirstIDX(ctx context.Context) int {
	id, err := sccq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single StatsCacheCrawler entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one StatsCacheCrawler entity is found.
// Returns a *NotFoundError when no StatsCacheCrawler entities are found.
func (sccq *StatsCacheCrawlerQuery) Only(ctx context.Context) (*StatsCacheCrawler, error) {
	nodes, err := sccq.Limit(2).All(setContextOp(ctx, sccq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{statscachecrawler.Label}
	default:
		return nil, &NotSingularError{statscachecrawler.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (sccq *StatsCacheCrawlerQuery) OnlyX(ctx context.Context) *StatsCacheCrawler {
	node, err := sccq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only StatsCacheCrawler ID in the query.
// Returns a *NotSingularError when more than one StatsCacheCrawler ID is found.
// Returns a *NotFoundError when no entities are found.
func (sccq *StatsCacheCrawlerQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = sccq.Limit(2).IDs(setContextOp(ctx, sccq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{statscachecrawler.Label}
	default:
		err = &NotSingularError{statscachecrawler.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (sccq *StatsCacheCrawlerQuery) OnlyIDX(ctx context.Context) int {
	id, err := sccq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of StatsCacheCrawlers.
func (sccq *StatsCacheCrawlerQuery) All(ctx context.Context) ([]*StatsCacheCrawler, error) {
	ctx = setContextOp(ctx, sccq.ctx, ent.OpQueryAll)
	if err := sccq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*StatsCacheCrawler, *StatsCacheCrawlerQuery]()
	return withInterceptors[[]*StatsCacheCrawler](ctx, sccq, qr, sccq.inters)
}

// AllX is like All, but panics if an error occurs.
func (sccq *StatsCacheCrawlerQuery) AllX(ctx context.Context) []*StatsCacheCrawler {
	nodes, err := sccq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of StatsCacheCrawler IDs.
func (sccq *StatsCacheCrawlerQuery) IDs(ctx context.Context) (ids []int, err error) {
	if sccq.ctx.Unique == nil && sccq.path != nil {
		sccq.Unique(true)
	}
	ctx = setContextOp(ctx, sccq.ctx, ent.OpQueryIDs)
	if err = sccq.Select(statscachecrawler.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (sccq *StatsCacheCrawlerQuery) IDsX(ctx context.Context) []int {
	ids, err := sccq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (sccq *StatsCacheCrawlerQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, sccq.ctx, ent.OpQueryCount)
	if err := sccq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, sccq, querierCount[*StatsCacheCrawlerQuery](), sccq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (sccq *StatsCacheCrawlerQuery) CountX(ctx context.Context) int {
	count, err := sccq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (sccq *StatsCacheCrawlerQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, sccq.ctx, ent.OpQueryExist)
	switch _, err := sccq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (sccq *StatsCacheCrawlerQuery) ExistX(ctx context.Context) bool {
	exist, err := sccq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the StatsCacheCrawlerQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (sccq *StatsCacheCrawlerQuery) Clone() *StatsCacheCrawlerQuery {
	if sccq == nil {
		return nil
	}
	return &StatsCacheCrawlerQuery{
		config:     sccq.config,
		ctx:        sccq.ctx.Clone(),
		order:      append([]statscachecrawler.OrderOption{}, sccq.order...),
		inters:     append([]Interceptor{}, sccq.inters...),
		predicates: append([]predicate.StatsCacheCrawler{}, sccq.predicates...),
		// clone intermediate query.
		sql:  sccq.sql.Clone(),
		path: sccq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		EntityType statscachecrawler.EntityType `json:"entity_type,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.StatsCacheCrawler.Query().
//		GroupBy(statscachecrawler.FieldEntityType).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (sccq *StatsCacheCrawlerQuery) GroupBy(field string, fields ...string) *StatsCacheCrawlerGroupBy {
	sccq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &StatsCacheCrawlerGroupBy{build: sccq}
	grbuild.flds = &sccq.ctx.Fields
	grbuild.label = statscachecrawler.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		EntityType statscachecrawler.EntityType `json:"entity_type,omitempty"`
//	}
//
//	client.StatsCacheCrawler.Query().
//		Select(statscachecrawler.FieldEntityType).
//		Scan(ctx, &v)
func (sccq *StatsCacheCrawlerQuery) Select(fields ...string) *StatsCacheCrawlerSelect {
	sccq.ctx.Fields = append(sccq.ctx.Fields, fields...)
	sbuild := &StatsCacheCrawlerSelect{StatsCacheCrawlerQuery: sccq}
	sbuild.label = statscachecrawler.Label
	sbuild.flds, sbuild.scan = &sccq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a StatsCacheCrawlerSelect configured with the given aggregations.
func (sccq *StatsCacheCrawlerQuery) Aggregate(fns ...AggregateFunc) *StatsCacheCrawlerSelect {
	return sccq.Select().Aggregate(fns...)
}

func (sccq *StatsCacheCrawlerQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range sccq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, sccq); err != nil {
				return err
			}
		}
	}
	for _, f := range sccq.ctx.Fields {
		if !statscachecrawler.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if sccq.path != nil {
		prev, err := sccq.path(ctx)
		if err != nil {
			return err
		}
		sccq.sql = prev
	}
	return nil
}

func (sccq *StatsCacheCrawlerQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*StatsCacheCrawler, error) {
	var (
		nodes = []*StatsCacheCrawler{}
		_spec = sccq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*StatsCacheCrawler).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &StatsCacheCrawler{config: sccq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, sccq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (sccq *StatsCacheCrawlerQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := sccq.querySpec()
	_spec.Node.Columns = sccq.ctx.Fields
	if len(sccq.ctx.Fields) > 0 {
		_spec.Unique = sccq.ctx.Unique != nil && *sccq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, sccq.driver, _spec)
}

func (sccq *StatsCacheCrawlerQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(statscachecrawler.Table, statscachecrawler.Columns, sqlgraph.NewFieldSpec(statscachecrawler.FieldID, field.TypeInt))
	_spec.From = sccq.sql
	if unique := sccq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if sccq.path != nil {
		_spec.Unique = true
	}
	if fields := sccq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, statscachecrawler.FieldID)
		for i := range fields {
			if fields[i] != statscachecrawler.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := sccq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := sccq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := sccq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := sccq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (sccq *StatsCacheCrawlerQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(sccq.driver.Dialect())
	t1 := builder.Table(statscachecrawler.Table)
	columns := sccq.ctx.Fields
	if len(columns) == 0 {
		columns = statscachecrawler.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if sccq.sql != nil {
		selector = sccq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if sccq.ctx.Unique != nil && *sccq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range sccq.predicates {
		p(selector)
	}
	for _, p := range sccq.order {
		p(selector)
	}
	if offset := sccq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := sccq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// StatsCacheCrawlerGroupBy is the group-by builder for StatsCacheCrawler entities.
type StatsCacheCrawlerGroupBy struct {
	selector
	build *StatsCacheCrawlerQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (sccgb *StatsCacheCrawlerGroupBy) Aggregate(fns ...AggregateFunc) *StatsCacheCrawlerGroupBy {
	sccgb.fns = append(sccgb.fns, fns...)
	return sccgb
}

// Scan applies the selector query and scans the result into the given value.
func (sccgb *StatsCacheCrawlerGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sccgb.build.ctx, ent.OpQueryGroupBy)
	if err := sccgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StatsCacheCrawlerQuery, *StatsCacheCrawlerGroupBy](ctx, sccgb.build, sccgb, sccgb.build.inters, v)
}

func (sccgb *StatsCacheCrawlerGroupBy) sqlScan(ctx context.Context, root *StatsCacheCrawlerQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(sccgb.fns))
	for _, fn := range sccgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*sccgb.flds)+len(sccgb.fns))
		for _, f := range *sccgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*sccgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sccgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// StatsCacheCrawlerSelect is the builder for selecting fields of StatsCacheCrawler entities.
type StatsCacheCrawlerSelect struct {
	*StatsCacheCrawlerQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (sccs *StatsCacheCrawlerSelect) Aggregate(fns ...AggregateFunc) *StatsCacheCrawlerSelect {
	sccs.fns = append(sccs.fns, fns...)
	return sccs
}

// Scan applies the selector query and scans the result into the given value.
func (sccs *StatsCacheCrawlerSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, sccs.ctx, ent.OpQuerySelect)
	if err := sccs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*StatsCacheCrawlerQuery, *StatsCacheCrawlerSelect](ctx, sccs.StatsCacheCrawlerQuery, sccs, sccs.inters, v)
}

func (sccs *StatsCacheCrawlerSelect) sqlScan(ctx context.Context, root *StatsCacheCrawlerQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(sccs.fns))
	for _, fn := range sccs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*sccs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := sccs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
