# Generate one high-entropy value, then copy this file to deploy/.env on the
# server. Use the same value locally as SILAN_STATS_SYNC_TOKEN when launching
# Silan Desktop or CLI stats sync.
STATS_SYNC_TOKEN=replace-with-at-least-32-random-bytes
