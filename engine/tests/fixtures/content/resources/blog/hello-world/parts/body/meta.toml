# Part identity for the blog `body` part (per 01 §1.3.1).
# `part_id` is omitted in fixtures: the scan generates one, which is the
# documented behaviour for a part that has not yet been scaffolded.
type           = "body"
shape          = "prose"
canonical_lang = "en"
