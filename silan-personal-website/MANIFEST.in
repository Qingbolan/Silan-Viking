# The `silan` package is a thin entry-point shim — three small .py
# files, no bundled binaries, templates, or skills. Nothing extra to
# package beyond the source itself.
include README.md
include LICENSE
include pyproject.toml

global-exclude __pycache__
global-exclude *.py[co]
global-exclude .DS_Store
