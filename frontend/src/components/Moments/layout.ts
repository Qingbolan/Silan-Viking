/**
 * Shared horizontal frame for the Moments hero and timeline.
 *
 * Keeping the width and gutters in one token prevents the cover copy,
 * profile identity, and timeline rail from drifting onto different axes.
 */
export const MOMENTS_CONTENT_FRAME_CLASS =
  'mx-auto w-full max-w-[1280px] px-4 sm:px-8';
