/**
 * Shared horizontal frame for full-width editorial pages.
 *
 * Page-level heroes and section stacks use the same width and gutters so
 * their content axes stay stable while the reader scrolls between sections.
 */
export const EDITORIAL_CONTENT_FRAME_CLASS =
  'mx-auto w-full max-w-[1280px] px-4 sm:px-8';
